# D1.02 恢复现场

本文件只记录未完成现场，不代表包级通过。沿原v3.3规划继续内核；AI自我复核和自动验收，无人工批准节点。G0与D1.01已Passed，不重新开启；D1.02仍InProgress。

## 已提交基线与当前工作区

- 分支：`work/d0-kernel-baseline`；已推送HEAD：`8cd018c`，API/expected初始冻结节点。远端仍是用户指定Kernel_v2。
- 本轮代码、文档和过程证据均留在工作区，尚未满足最终审核/矩阵条件，没有提交推送未验收实现。不要将“Implemented”目标元数据误读为包级Passed。
- 独立执行者 implement_review_policy、代码审核者 implement_d004 返回Codex usage limit错误，本轮无法继续其独立复核。规格审核者此前已完成Target增量批准。没有消费用量重置信用、没有转人工验收、没有创建后台定时任务。
- 当前源码快照：`evidence/bootstrap/D1.02/integration-checkpoint-7c7d5c0382/current-source.zip`。同目录架构实际图、完整架构unittest和git diff --check均正常退出；这不是正式包级矩阵。

## 本轮实现与真实结果

- 六个CoreContracts公开头、33个原生主体函数、独立每配置发现、11项附加编译/故障子流程及确定性工厂已落地。根CMake已加入CoreContracts安装和测试；仅Foundation/CoreContracts目标标为Implemented，Runtime等仍ContractBaseline且runtime_available=false。
- 父任务的T01两组守卫及T24三个安装消费者已接入。公开宏定义现显式登记为空，实际CMake图精确比较definitions/features/options；宏漂移red及17项定向green已保留。
- SDK驱动读取实际生产者cache传播ASan，去RTC、使用Embedded、禁增量，运行时采用实际编译器目录，并检查真实CL命令追踪中的插桩。宏消费者同时检查min/max原展开值。两项驱动守卫通过；实际SDK三用例仍未取得green。
- 原生首轮因factories先于test_support包含而失败，已修正包含顺序。随后仅剩拥有性声明static_assert失败，见`native-order-fixed-4295510649`；修复identity中的owning()为精确AsyncOwnership类型且可用于integral_constant，实际拥有性主体通过。
- `native-owning-fix-be95bfea19`中的资源owner别名主体确实失败。WorkContext现从const vector引用复制shared owner元素，不接管调用者可保留元素别名的缓冲；后续该主体通过。
- `native-owner-green-c2e7eddda4`实际发现33项并逐个执行，32项主体退出0，观察同版本进度冲突1项失败；不含附加编译/故障子流程，不能计作33项CTest通过。
- 观察同版本检查已补progress/evidence/record_state/writes_blocked/repair/fault比较，保留同版完整事实补齐；`native-observation-green-816c4d0fac`构建及该主体退出0。尚未在此最终源码上重跑整套33主体。

## 编译环境：已找到可行对照，尚未正式集成

- 原安装cl完成后创建常驻vctip.exe，导致owned Job严格判DescendantsAlive。不是MSBuild node reuse或/Zi的单一问题。`telemetry-env-08a3aa78eb`记录原始、VSTEL_EWDK=1、EnterpriseWDK=1均失败；不得重写或当作合同负例。
- 共享vctip服务方案经只读评估有固定全局IPC影响，未启动该服务方案、未改注册表、未修改原安装、未终止外部进程。
- 已在工作区复制锁定Hostx64/x64目录，保留90个文件原始字节，唯一不复制vctip.exe。原始来源为 `F:/VS2022/VS2022/VC/Tools/MSVC/14.44.35207/bin/Hostx64/x64`。副本在 `build/d1.02-tools-probe-55d5771dfc/bin`，逐项SHA见 `local-tools-55d5771dfc/copy-inputs.json`。
- 副本直接cl /c /Z7正常退出、Job无遗留。`local-tools-cmake-55d5771dfc`还实际完成fresh CompilerId、ABI、静态归档、链接与运行，四工程查询CLToolPath/LinkToolPath/LIBToolPath/ExecutablePath均指副本，CMAKE_CXX_COMPILER也报告真实副本路径。原始输出与probe-artifacts.zip已保存；工具二进制仅在build缓存，不进Git。
- 使用ForceImportBeforeCppTargets导入临时 `build/d1.02-tools-probe-55d5771dfc/local-tools.props`。该属性文件同时设置CL/Link/LIBToolPath并前置ExecutablePath及非空NativeExecutablePath；只改CLToolPath会让CMake报告原安装路径，不能采用。
- `cmake/LockedMSVC.cmake`尚未修改为自动使用副本。`build/d1.02-debug-integration`配置时显式追加了上述ForceImportBeforeCppTargets；普通独立SDK/编译子消费者仍会走原工具路径，因此暂勿直接跑正式矩阵。
- 后续须把局部工具物化/逐文件SHA验证/真实路径核对做成可复核的开发配置，保持版本/SDK/CRT和原始二进制字节，不弱化进程树规则；再覆盖实际Debug/Release/ASan及独立消费者。禁止把临时副本路径写入公开SDK导出。

## Target授权增量：规格通过，代码未落实

- 初版代码审核 `code-review-identity-context-initial.md`列三项，拥有性与owner别名已有修复实证，TargetAuthority仍未实现。
- 已审提案 `target-authority-proposal.md` SHA c10db74d135df3dee9abebfe291d15be42f4a20be40ffabd6f7bb3d6bea05dde。
- 独立批准 `spec-review-final-target-authority.md` SHA e56b10f7ab927b6d4d05193c29d400e455b2e02981f5308342880eee5f6deea8；初版意见保留。
- 正式API已迁入3.1.1，当前SHA 34fe36860dd33079f754eda209af9ab0d5093dfcda6ab10c81cbf52a25b53d3d；增量绑定见 `docs/reviews/D1.02-api-target-authority.md`。原D1.02-api.md仍真实绑定8cd018c的旧API，不改写历史。
- context.hpp当前仍有TargetView::revalidate和公开EffectContext/TransitionView构造，需要先补实际反例再按新API改为TargetAuthorityPort resolve/validate、私有构造和check/revalidate。真实接收者提供自己的两个authority/current PermitBinding/current_before，不能用permit自报绑定作expected；不提前consume。
- 在既有context_capability_boundary/effect_shape/lifecycle_shape内补同issuer A/B交换permit/target grant拒绝且consume=0、跨issuer/伪grant/撤销/过期/当前operation或权限/生命周期/before改变拒绝。38名称不变。
- 继续完整代码审核时也应核对测试工厂的发放记录私有性、实际支持头依赖和实现摘要，不以工厂内部自报成功代替真实接口约束。

## 后续验收顺序

1. 完成受控工具环境集成与审核，再落实Target补充API及其red/green；运行全部33原生主体和附加编译/故障子流程、三个真实SDK消费者。
2. 每次公开头变更后更新sdk_api_manifest实际SHA；当前已与保存快照同步，但不能对后续修改沿用旧SHA。
3. 准备D1.02三份正式run manifest（目前尚未创建），固定expected仍是新增38项、含对应既有回归共191/191/193，另有python依赖、bootstrap Conformance、Outcome一致性三CHECK。可以开G0注册，但使用固定expected的精确过滤，不能从发现反填expected。
4. 独立AI规格/代码审核绑定最终来源，提交一个已审实现节点，再在同一commit及同一来源摘要上采完整三配置。不要在矩阵中改源码或切HEAD。完整CTest、CHECK、原始子命令及实际编译/运行产物归档；失败重跑追加。
5. 自动门禁Passed后更新中文文档与进度，按必要节点提交并推送任务分支。Python验证器使用规范sys.executable绝对路径，避免此前.EXE/.exe逐字argv差异。

恢复时先读取本文件、docs/progress.md和当前git状态，确认有没有用户新增修改；本轮不存在需人工批准的设计问题。


## 2026-09-07 继续执行：最终审核与正式矩阵准备

以下更新替代上文恢复点的当前状态，上文作为历史保留。TargetAuthority补充实现、拥有性与观察快照修复已落实；六头公开合同没有扩展生产Runtime。确定性接收者现保存上下文owner及创建时原始许可，伪许可替换不能借用真实许可消费；发行集合及具体发行类型已私有化。独立复核 `evidence/bootstrap/D1.02/review-final-contracts-20260907-fixes-approved.md` 为AI Approved，旧反例在独立重新编译运行中均正确拒绝，历史失败保留。

局部锁定MSVC副本已集成，90文件逐字节校验、整目录原子发布、只读复用及Windows扩展路径等价性均经实际验证；不修改原安装或放宽进程树要求。`integration-ctest-a387f5757c` 的38项集成检查全部通过（早于最终测试工厂修复）；`asan-sdk-probe-b8d1960213` 的实际ASan公共头消费者通过。最终修复后 `review-fixes-green-b719840ef2` 的5项缓存检查、扩展路径旧反例、33项原生主体全部通过；独立工具复核另有并发与来源SHA实证。上述均为局部事实，不替代正式矩阵。

已准备三份D1.02正式run manifest，固定expected仍为191/191/193及每配置三CHECK；归档包含真实SDK安装/裁剪包、编译追踪、正例对象及三文件工厂组合摘要。归档模式增加明确层级以兼容采集器glob与独立validator的Path.match，仍保留递归采集。正式运行须在同一已审提交与输入摘要上执行；失败追加保留。D1.02当前仍InProgress，未开始D1.03。
