# D1.01 Foundation 验证记录

当前包级状态：Passed。前置D0.03、D0.06及G0已Passed；本包按用户明确的[AI复核与自动验收政策](../reviews/automatic-acceptance-policy.json)推进，无人工验收要求。只交付Foundation原语和必要SDK验证。

## 实际实现

公开入口为 `ock/foundation/foundation.hpp`，单一tl::expected 1.1.0别名后端；包含错误码/不可变拥有型详情、ASCII名称、强类型128位身份、registry世代句柄、epoch与受检算术/预算计数。Foundation实现状态前进，其余20个组件仍保持合同目标。安装导出锁定expected头及许可证，并以搬迁后的安装树真实编译消费。

## 先行验证与审核修复

- SDK首轮11项全部通过（新增4项、基础7项），原始JUnit和116个子进程文件见 `evidence/bootstrap/D1.01/sdk-first-9c3d7844aa`。
- ASan集成首轮26项通过（23个Foundation原语、2个检测控制例、1个SDK元数据），见 `evidence/bootstrap/D1.01/asan-integration-green-7006e93701`。
- 原语独立审核补齐空详情零分配、fail-fast no-op必须拒绝、ASan运行库搜索路径及VS多配置测试路由。原始失败与修复后Debug/ASan各23项及多配置正反检查见 `evidence/bootstrap/D1.01/20260907T084149Z-review-fixes-final-6db65dd8`。
- 独立代码审核实际复现ErrorInfo公开复制/赋值生成可变别名、改变已发布详情；反例见 `evidence/bootstrap/D1.01/independent-alias-probe-9027715f95`。四种复制/移动操作已显式删除，Error共享拥有权的复制/移动仍可用。原样消费者重新编译被C2280拒绝，见 `evidence/bootstrap/D1.01/independent-alias-recheck-099a57e3d1`；本包ownership固定反例见 `evidence/bootstrap/D1.01/immutable-details-red-e1f8a2b816`。

早期轮次只证明当时源码和局部检查，不代替下述最终矩阵。所有失败轮次保留，不覆盖退出码或日志。

## 最终验收要求

固定expected集合来自先行实施计划：Debug/Release各34项，ASan36项；原生23项、SDK新增4项、基础回归7项，ASan另含2项检测控制例。每配置独立构建目录和唯一run，归档来源、实际工具链/二进制、CTest发现、JUnit、安装与故意失败子进程材料。三个配置使用同一源输入摘要，不以发现结果反向削减固定预期。

独立[规格审核](../../evidence/bootstrap/D1.01/spec-review.md)及[不可变性增量审核](../../evidence/bootstrap/D1.01/spec-review-immutability.md)通过；独立[代码审核](../../evidence/bootstrap/D1.01/code-review.md)已通过。不可变性最终修复后Debug/Release/ASan各23项原生测试全部通过，见 `evidence/bootstrap/D1.01/immutable-details-green-67056e8c89`；35项架构检查也通过。完整正式矩阵归档前包级仍InProgress。

## 首轮正式矩阵与环境隔离修复

来源 `98a406f2b6121ea6089c391d6edc8b79f5e2d99f`、输入摘要 `1728e978a21b7cedce112e9e5c63018d44797993bd6eb60681b7b6a7a231dd39` 的三份报告均保留于 `evidence/98a406f2b612-1728e978a21b/`。Debug、Release各34/34；ASan为34/36，其中 `T24.foundation.installed_consumer` 与 `T24.foundation.missing_dependency_rejected` 的独立配置超过180秒。整包[首轮汇总](../../evidence/D1.01/automatic-20260907T090632Z/gate-summary.json)为Failed，不能放行。

发现并局部隔离用户vcpkg自动链接及VLD属性注入，详见[工具链说明](../toolchain.md)。仅设置globals的早期实验未传播到ABI，完整机制实验实际三类属性及消费者编译/运行通过；最终驱动首次MSBuild路径提取失败也保留于 `isolation-integration-ee8812ee3f`，随后改用生成器提供的真实路径。修复不提高超时，不修改全局设置或其他项目，不改原失败报告。最终源码重新AI复核、提交来源和全部三配置实测后才能另行自动验收。

局部修复验证追加：`evidence/bootstrap/D1.01/isolation-integration-eeeb45b739/` 四项SDK真实正反例均退出0并归档子命令；代码复核指出CompilerId默认查询配置可能落到Win32，已为识别/ABI明确Debug/x64、消费者明确当前配置/x64，保持两项被验证属性不受参数覆盖。最终 `configured-headers-0a93f8affa/` 实际Release公开头、三类属性查询、编译和运行退出0，驱动摘要与最终源码一致。该局部证据不代替完整矩阵。

## 最终三配置自动验收

实现提交 `171907d64bf396fef831348f8082c5b042ea31dc`，163项来源均与Git对象逐字节一致；共同输入摘要 `2feab22721bc96a47006a4e0f955ebe79ebf036266d7387ec85d71893461b692`。Debug 34/34、Release 34/34、ASan 36/36，共104次CTest全部通过，含ASan健康/真实越界控制。报告位于 `evidence/171907d64bf3-2feab22721bc/`，均无证据校验错误。

[最终门禁](../../evidence/D1.01/automatic-20260907T100928Z/gate-summary.json)实际为Passed；[验收上下文](../../evidence/D1.01/automatic-20260907T100928Z/acceptance-context.json)明确使用AI规格/代码复核、无需人工。旧工具生成的通用说明仍保留，实际每个run的必需复核为spec/code。验收只覆盖D1.01，G1不因此自动放行。

汇总首次以启动器解析出的`python.EXE`运行，和已绑定`python.exe`的argv精确比较不符，失败记录保留于 `automatic-20260907T095653Z`。核实为相同解释器文件/hash后，改用报告绑定的完整路径调用原工具，重新汇总通过；没有修改校验规则、原报告或重写失败。本限制仅关乎汇总启动路径，三配置测试来源和结果保持原样。

AI审核链保留原规格/不可变性/代码审核，并追加构建隔离、查询配置和独立代码增量审核，正式记录绑定同一163项来源。D1.02的全部前置另见[启动复核](../reviews/D1.02-start.json)，继续限定为内核合同及规划验证消费者。
