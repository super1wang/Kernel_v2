# D1.06 验证记录


**本次收口覆盖：按用户最新要求停止测试，仅开发提交并暂停；未验收。下文局部事实保留，待办不再执行。详见[开发交付记录](D1.06-development-delivery.md)。**
更新：2026-09-08。**包级 InProgress，G1 InProgress**。仅内核及规划要求的验证消费者；用户要求本包提交推送后暂停，不启动后续工作包。审核入口见[当前索引](../reviews/D1.06-index.md)。本页区分开发事实、审核结论与正式验收，不把不同源码的局部成功拼为一次正式通过。

## 已有开发事实

| 范围 | 实际结果 | 精确来源入口 |
|---|---|---|
| Host 首批固定主名 | Debug 20项，expected/discovered/executed相等；包含真实析构正负控制 | `host-ctest-59106d6953`，构建 `sdk-stage-8f6e72bfae` |
| 截止与真实日志业务隔离 | 两项增量通过；handler内日志异常不改变真实ReadCompleted值 | `sdk-stage-41f224b68b` |
| 启动故障、模块日志、会话移动/撤权、生命周期并发 | 五项增量通过 | `sdk-stage-754054ec5d` |
| 目录拒绝、会话名额、清理截断、move-only/void结果 | 五项增量通过 | `sdk-stage-3593be88f8` |
| Runtime 合库/规范头搬迁的传递影响 | 复用同一配置构建，Debug注册25、Policy35、Native32，共92项通过；逐family固定集合对照 | `host-impact-770d3cc66d`，复用 `3593be88f8` |
| Host Release 风险集 | 返回/准入/稳态分配/会话/日志五项通过，另同EXE分配校准通过 | `sdk-stage-e353eebb66`、`host-ctest-9bc913f314` |
| Host ASan 风险集 | 同五项通过，另同EXE真实12入口与注册失败校准通过 | `sdk-stage-94e68a6578`、`host-ctest-975296ac45`、`host-ctest-0eece3deb5` |
| 启动故障补充 | facade/Policy分配失败后实际清理，raw日志close错误/异常重试，内部Policy先关闭及饱和控制，公开关闭异常通过 | `host-fault-followup-c925c3b707`，复用 `sdk-stage-da08b48728` |
| Logging | 两合法后端共同10项及owner修复4项有各自来源；新增分页/分配及格式/计数6项通过，两后端各80固定/满载窗口cpp/crt为0 | [独立CODE报告](../../evidence/bootstrap/D1.06/logging-independent-code-review.md) |
| SDK | 独立安装、裁剪、无测试生产者、链接/真实运行及构建身份专项通过；detail包含边新增实际red后49护栏通过 | [独立CODE报告](../../evidence/bootstrap/D1.06/sdk-independent-code-review.md) |

上述短名均位于 `evidence/bootstrap/D1.06/`，各自有源码/命令/原始流或JUnit摘要。Release/ASan专项与Debug回归不冒充正式三配置完整集合。分配通道分别判定，Release CRT不可用不补零；用户外壳/结果释放与Host准入寿命按合同区分。

## 保留的失败及诊断

- `sdk-stage-acd5981720`：实际已过期shutdown仍触发日志回调；修复后仅对应两项增量重测。
- `sdk-stage-2c966cd5ec`：新增测试复用旧分配校准的头包含顺序错误；修正后新轮编译。不归类为生产行为失败。
- `host-ctest-0758cb5a25`：全工程发现被无关未构建Foundation阻断，后续限定已构建Host子目录。
- `sdk-stage-94e68a6578`：初次直接启动ASan EXE缺运行库搜索路径；同一二进制通过已有CTest进程局部PATH运行成功，没有重编或修改全局环境。
- `sdk-stage-450f302dcb`：故障测试ObjectId未限定导致编译错误；新轮修复。
- `sdk-stage-da08b48728`：Debug create逐次分配注入第2项进入终止，**该全扫未通过**。已由锁定MSVC源码、独立vector控制及适用性审核确认容器代理noexcept平台边界；不能把进程退出当Host清理成功，也不按分配大小跳过。
- Logging测试后端固定格式最初每80窗cpp/crt各1200次分配，真实red保留；改独立有界格式后实测零，生产Logging未为此改写。

## 尚未完成的验收工作

1. Host故障平台适用性已获限定审核；三配置实际包装在 `host-wrapper-check-6cf27412d0` 全部通过。包装七种诊断污染/退出事实不一致反例已先红后绿，最终增量CODE结论待独立确认。
2. footprint v2已经具备实际同安装消费者单对，内存按5ms到期尝试、线程仅固定阶段采样，原始缺样明确保留；分配模式和线程归因仍待完成，pilot、独立有限预算及预算后新报告尚未开始。局部控制Passed不等于测量方法或G1通过。
3. 最终稳定来源的正式S_required/S_gate、精确集合与证据归档、自动包级验收尚未完成；不临时删除冻结项取得Passed。

D1.05原正式报告未重跑或改写。此前工具节点为 `af5ff2b`，本包生产实现尚未提交；只有本包收口提交推送完成后才进入用户要求的暂停。
