# D1.02 CoreContracts 验证登记

当前包级状态：InProgress。全部前置已按[启动记录](../reviews/D1.02-start.json)核实Passed。本页登记实施前预期，尚无D1.02编译或运行通过结论。

## 固定范围与计数

新用例取自[已审计划](../plans/D1.02.md)中固定的38项；[expected清单](../../tests/manifests/d1.02.expected.json)在实现和发现之前登记。实际测试注册不得读取此清单产生用例，不能将发现结果反向写成预期。

| 分组 | Debug | Release | ASan | 语义 |
|---|---:|---:|---:|---|
| D1.02新增合同 | 38 | 38 | 38 | 类型/shape、绑定、Outcome、窄端口与确定性工厂、观察及安装消费 |
| D1.01与既有基线 | 34 | 34 | 36 | 已验收Foundation和架构/依赖/SDK，ASan另含健康和真实越界控制 |
| D0.03 | 43 | 43 | 43 | Python Outcome及完成寿命参考模型回归，不称为原生实现 |
| D0.05 | 63 | 63 | 63 | Python提交/许可/Intent参考模型回归，不称为实际存储 |
| D0.06共同规则 | 13 | 13 | 13 | 原bootstrap工厂资格和规则保护，不称为生产Executor |
| 合计预期 | 191 | 191 | 193 | 尚未运行，计数不是Passed |

另有`CHECK.dependencies.python`、`CHECK.conformance.bootstrap`和`CHECK.models.outcome_consistency`三个非CTest检查，分别验证实际开发依赖、旧共同harness的真实工厂结果，以及D0.05实际结果对D0.03 Schema/语义的一致性，不混入上表次数。D1.02原生端口工厂版本另行登记，不能覆盖旧`ock.executor.bootstrap/1`证据或用工厂能力开关关闭必需规则。

## 后续运行约束

最终公开头为已审计划的六个候选入口，待具体声明AI规格审核后实施并冻结实际摘要；不能先把不存在的头标成已安装。使用D1.01已实测的LockedMSVC、配置化测试注册、实际ASan运行库目录和Windows Job子进程证据。编译拒绝先有同工具链成功控制，再检查真实目标诊断；失败、超时、启动失败均保留并拒绝放行。

正式三配置必须使用同一提交与源输入摘要；包含真实CTest发现/JUnit、实际编译配置、二进制、子进程控制材料及AI规格/代码审核。门禁汇总使用报告绑定的完整Python解释器路径，避免Windows启动器大小写差异导致精确argv核对失败，不放宽证据规则。

## 实施前规格冻结

[具体API](../contracts/core-contracts-api.md)及[独立规格审核](../reviews/D1.02-api.md)已完成；固定expected另有[集合审核](../reviews/D1.02-expected.md)。审核补齐实际候选读取适配、Outcome决定材料、跨发布器接收重验、旧事实完整前缀与真实取消决定核对。六个公开头和38项名称保持不变。接下来先写真实反例再实施；当前仍没有D1.02原生测试通过结论。


## 首轮实现与增量自我复核（尚未验收）

- 已实现阶段/闭包/六头边界守卫；首轮真实 red 与后续 15 项 green 保存在 `evidence/bootstrap/D1.02/architecture-*`。增量规格审核进一步发现公共编译宏未登记，已补具体漂移反例；`architecture-macros-red-5ee7bef390` 为 11 项中 2 项失败，修复后 `architecture-macros-green-6a0fd178db` 为 17 项通过。尚待实际 CMake 导出与最终六头清单集成。
- 安装可用性有真实红灯：`d1.02-component-red-4d2eccf27a` 对同一旧 SDK 的 Foundation 配置成功、CoreContracts 以未实现组件拒绝。这仅证明需补组件导出，不计原生编译通过。
- 新 SDK 驱动已覆盖六头独立包含、宏原展开保持、搬迁/无测试生产者、成功控制后裁剪 Foundation/expected，以及未实现组件拒绝。独立规格审核见 `spec-review-integration-initial.md`：公共宏与 ASan 传播两处必须修复。驱动已按生产者实际 cache 传播 ASan，并核对真实 CL 命令追踪中的插桩；`sdk-driver-guards-e00bef0f0f` 两项守卫通过，实际 SDK green 仍待采集。
- 原生构建环境仍有未关闭的阻断：编译器退出 0 后，其 `vctip.exe` 子进程常驻，独占 Job 严格记为 `DescendantsAlive`。既有 `/Z7`、禁 MSBuild node reuse 与两项局部环境变量对照均未解决。`sdk-red-e2af83c366` 的 Foundation 控制先失败，因此没有继续把 CoreContracts 的失败计作合同拒绝；全部原始命令/子证据保留。正在评估局部工具环境，不修改全局注册表、用户安装或他人进程，不放宽 Job 判定。
- identity/context 增量代码审核见 `code-review-identity-context-initial.md`，结论 ChangesRequired：资源 owner 容器移动后外部元素别名、AsyncOwnership 精确声明约束、目标视图配置 authority 边界。正在补反例和修复；TargetAuthority 具体化单独走 AI 规格复核，38 个固定用例名称保持不变。

以上均为追加的实施过程记录。D1.02 仍为 InProgress，没有正式 191/191/193 矩阵或包级 Passed。


## 本轮恢复点

局部原始字节工具副本已取得干净编译、CompilerId/ABI/静态归档/链接与真实路径一致性对照；尚未接入正式工具链。拥有性声明和资源owner别名已有实际修复，33主体首轮32通过/1失败后，观察同版本冲突单项已修复通过。Target补充API获AI规格批准并已登记，产品代码与反例尚待落实。独立执行/复核任务随后返回Codex用量限制，D1.02仍InProgress；未提交未验收实现。完整源码快照、具体路径及恢复步骤见[D1.02恢复现场](D1.02-resume.md)。


## 2026-09-07 继续执行：最终审核与正式矩阵准备

以下更新替代上文恢复点的当前状态，上文作为历史保留。TargetAuthority补充实现、拥有性与观察快照修复已落实；六头公开合同没有扩展生产Runtime。确定性接收者现保存上下文owner及创建时原始许可，伪许可替换不能借用真实许可消费；发行集合及具体发行类型已私有化。独立复核 `evidence/bootstrap/D1.02/review-final-contracts-20260907-fixes-approved.md` 为AI Approved，旧反例在独立重新编译运行中均正确拒绝，历史失败保留。

局部锁定MSVC副本已集成，90文件逐字节校验、整目录原子发布、只读复用及Windows扩展路径等价性均经实际验证；不修改原安装或放宽进程树要求。`integration-ctest-a387f5757c` 的38项集成检查全部通过（早于最终测试工厂修复）；`asan-sdk-probe-b8d1960213` 的实际ASan公共头消费者通过。最终修复后 `review-fixes-green-b719840ef2` 的5项缓存检查、扩展路径旧反例、33项原生主体全部通过；独立工具复核另有并发与来源SHA实证。上述均为局部事实，不替代正式矩阵。

已准备三份D1.02正式run manifest，固定expected仍为191/191/193及每配置三CHECK；归档包含真实SDK安装/裁剪包、编译追踪、正例对象及三文件工厂组合摘要。归档模式增加明确层级以兼容采集器glob与独立validator的Path.match，仍保留递归采集。正式运行须在同一已审提交与输入摘要上执行；失败追加保留。D1.02当前仍InProgress，未开始D1.03。


## D1.02 最终三配置验收

实现提交 `c6950773dd4134f39059ee5e9fa0acc12541ff96` 已推送，189项源码与Git字节逐项一致；输入摘要 `daad9b3588510afff1865f9717ea4d3da47bc788d375a8b585c6e67b29943aca`。正式Debug 191/191、Release 191/191、ASan 193/193，共575次CTest与9项CHECK全部通过。三份原始报告位于 `evidence/c6950773dd41-daad9b358851/`，自动状态、各run包级状态均Passed，校验错误为空。

最终当前版本门禁 `evidence/D1.02/current-20260907T150316Z/gate-summary.json` 实际Passed，AI规格与代码审核完整绑定同一来源，G1保持InProgress。首次误用历史候选入口只因matrix未在原来源快照被拒绝，完整保留 `evidence/D1.02/automatic-20260907T150134Z`；当前入口与D1.01一致，重新核验完整工作树、原始报告和三配置，未更改工具、来源或固定expected。验收上下文和Git来源核对另行归档。

已完成D1.02的六头合同、确定性验证消费者及必要开发工具；不声明Registry、Host或其他产品模块完成。下一包仅在核对本次Passed及规划后进入D1.03。以上替代旧恢复段落的当前状态，历史失败和审核意见保持原文。
