# D1.04 验证登记

当前状态：Passed。正式Debug/Release/ASan固定251/251/253、九CHECK、当前完整自动门禁及独立最终AI验收均已完成。下文按轮次保留历史事实，早期“待实现/待修/进行中”不代表最新状态。前置D1.02与D0.05的实际原始报告和最终验收SHA均已核对，见 `docs/reviews/D1.04-prerequisites.json`；D0.05原始InProgress字段保持历史字节，其最终Passed来自G0自动验收。

本包仅实现内存授权、会话委托、目标解析、一次性许可及观察/发送授权短仲裁；不扩展公开Runtime SDK、Host、生产传输或产品模块。计划及具体API独立AI复核通过后冻结反例集合，再编写行为实现。早期规划阶段没有D1.04测试通过计数；实际各轮结果与当前未验收边界见后文。


初次具体API/计划为PendingReview，原始字节保存在 `evidence/bootstrap/D1.04/draft-initial/`。独立规格初审 `docs/reviews/D1.04-api-initial.md` 为ChangesRequested，要求补权限逐use匹配/完整组访问/发送缓冲预留/查询响应发送接点/剩余声明。先修订再复核，当前无行为源码。候选35项集合位于 `evidence/bootstrap/D1.04/candidate-expected.json`，251/251/253仅为候选算术，不是已冻结实际expected或运行结果。


修订2五项初审缺口经独立AI复核关闭后，主集成追加的source寿命组合问题被独立确认：源host/同ExecutionRef授权事实若在查询与发送间重绑定，旧响应缺乏失效合同；增量ChangesRequested保存在 `evidence/bootstrap/D1.04/source-lifetime-review.md`。修订3固定源寿命，要求先经同一短仲裁close_store再更换源，旧queued/许可永久失效、已发生事实不回滚。`docs/reviews/D1.04-api-revision3.md` 与 `D1.04-plan-revision3.md` 为AI Approved，分别绑定修订3原字节；文档内PendingReview是送审时原文，最终准入由独立记录和后续start.json明确，不改受审字节。

实际expected与三份run manifests已准备，固定251/251/253及每配置三CHECK，五组驱动24子命令/48原始流/12生成cpp/4正控制obj布局经预审，完整清单仍在最终初始审核。当前尚无行为实现或测试Passed。

最终初始固定集合/清单审核Approved，见 `docs/reviews/D1.04-expected.md` 和 `evidence/bootstrap/D1.04/run-manifests-preflight.md`。`docs/reviews/D1.04-start.json` 绑定所有前置与批准材料，允许进入反例及行为实现；正式实际产物归档仍须集成后逐项复核。


规划冻结节点 `7a4316d` 已提交推送。首版头声明与四组控制的真实MSVC编译：4个positive退出0、8个negative按具体文件和预期诊断退出非0，13条owned命令及头快照见 `evidence/bootstrap/D1.04/compile-interface-initial`；173份构建材料ZIP逐字节核对，原解包目录移至忽略的 `build/d1.04-interface-initial-artifacts` 保留。这仅证明接口编译边界，不代表35主体或完整5wrapper行为通过。

新开发辅助脚本再次出现仅检查exit_code的缺口，原字节保留后修正；`develop-owned-controls` 的真实旧版本残留进程误判red、修后残留/超时/非零/干净退出四项断言全部符合预期。正式证据工具未改变。原生实现正在逐步推进，临时预算默认值检查不计实际拥有/预算行为通过。

## 原生切片与早期复核

`implementation-24ea10270e11` 首次实际构建并运行认证来源、四方scope切片；默认预算值启动检查不是预算合同验收。早期独立复核 `early-core-review/review.md` 提出三项需运行确认的问题，未将未实现方法当缺陷或作整包批准。

`implementation-636ddfd23169` 实际复现负epoch期限计算与重复委托规则总预算两个断言失败；`implementation-93640fc9eb51` 修后实际通过，并含缩减后容量恢复控制。每轮源码/命令/原始流独立保留；这些局部结果不代表完整owned_inputs_budget、expiry_boundary所有子断言或35主体验收。

`implementation-240bdc694b43` 实际复现存在但无权目标B可解析的问题，合法A为正控制。局部规格复核 `early-core-review/resolve-semantics.md` 发现无use/operation/owner/field的resolve签名需要明确候选可见性预检；API/计划修订4等待独立增量批准，相关行为修复暂停，其余实现继续。

自动审批曾拒绝“遍历全部配置主体owner决定目标可见性”的修改，理由为可能扩大未授权主体目标访问、弱化安全边界；该命令未执行，原始action/reason保存在 `implementation-target-approval-rejection/rejection.txt`。不得重试被拒绝的方案或以审核记录冒充运行成功。修订4拟议方案不枚举其他owner、不读取执行元数据，只检查同一目标候选，操作/查询/发送仍需完整tuple授权；尚须实际实现和复核。

修订4后续经 `docs/reviews/D1.04-api-revision4.md` 与 `D1.04-plan-revision4.md` 独立AI Approved，增量准入 `docs/reviews/D1.04-start-revision4.json` 绑定实际SHA并保留原start。现在允许按明确的四维共同候选定义修复，原始拒绝的owner枚举方案仍不得执行；批准不代表任何新增行为Passed。修订3与4各自原字节都已独立保留。

## 扩展切片与待关闭项

- 八项许可测试首轮 `action-tests-ca6981ffbee2` 实际全部退出0，独立审核 `action-tests-review.md` 仍为ChangesRequested：要求context替换经过真实接收者、正常摘要覆盖contract字节，以及动作重复拥有文本的总预算反例。不能以测试全绿代替规格复核。
- 后续CheckedEffect验证消费者私有保留context与创建时原permit，真实接收走revalidate、固定action消费和record_attempt；错误context记录0，正确context记录1且重复仍1。正常envelope/member contract单字节变化反例在 `action-tests-e53a4f20d9d9` 真实失败，等待修后独立增量审核。该消费者仅是D1.04验证接点，不是生产Invocation。
- 六项会话/服务/委托/精确契约/冻结目标测试 `session-tests-d1fd091cd49f` 实际全部退出0，`session-tests-review.md` 为限定AI Approved。报告明确全局generation与目标stamp分支的覆盖不能混称，不是整包批准。
- 五项发送测试 `send-tests-a07f008f5c47` 中发送先后、NotStarted重验、Unknown不重试通过；退订及关闭连接两项实际失败：queued_frames=1时旧Queued未及时删除，新的Watch/另一连接不能排入。实现者正在修复，原始失败保留，尚不标这两项Passed。
- 集成驱动来源稳定预审发现漏掉运行中新加文件，旧实现及实际red保留于 `integration-driver-before-source-fix`、`integration-source-controls-3fc26a2290`。修后完整inputs集合的新增/删除/修改/不变控制符合预期，`integration-driver-source-fix-review.md`限定Approved；尚未启动35主体完整集成或正式三配置矩阵。

各切片目录都保留独立源码快照、命令状态和原始stdout/stderr；这些开发记录不替代正式同提交/同来源摘要的251/251/253及九CHECK验收。

异常注入挂起已通过独立诊断定位至MSVC Debug默认空vector的noexcept内部代理分配，见 `evidence/bootstrap/D1.04/exception-root-cause.md`。20秒Timeout与诊断terminate observer实际Exited99均保留；尚需实现修复后重跑，不将该事件误写为仲裁死锁或Passed。

## 2026-09-08 冻结候选与完整审核

以下为当前状态，前文待修复段落保留历史事实。本包仍为InProgress。修订5固定同一Store内精确OperationKey到ContractDigest的终身映射，同Key改digest必须原子拒绝，已发行许可及响应保持有效；同digest的ACL更新仍允许。增量规格与计划批准见 `docs/reviews/D1.04-start-revision5.json`，旧修订记录保持原字节。`session-tests-163f73aa6cc4` 实际复现重绑定缺口，最终原生轮验证修复。

最终实施者冻结轮 `evidence/bootstrap/D1.04/implementation-8e072f1ca34a` 实际35个原生主体通过，含构建与实际枚举共37条命令全部Exited/0，active_after=0；冻结SHA及问题链见 `implementation-freeze-36315fd680b0/review.md` 与 `sources.json`。这只属于Debug开发验证，不替代根工程包装或正式251/251/253矩阵。

许可完整摘要、真实接收者及文本预算经 `action-tests-fix-review.md` 限定AI Approved。认证返回与委托嵌套输入别名测试经 `authority-tests-fix-review.md` 限定AI Approved；真实复制/move故障对照分别保存在 `authority-tests-ee994d69b049` 与 `authority-tests-5753280c8541`，故障仅作用于独立源码快照。大块MSVC容器释放地址探针失败属于测试控制偏差，不计内核缺陷。

Unknown遗漏同会话其他排队帧由 `queue-lifetime-probe-5bd109a08c13` 实际复现，正常close对照pending=0、Unknown旧实现pending=2。当前实现统一摘除并在锁外释放，最终35原生轮包含立即pending=0和其他连接恢复排入两个帧的断言；仍待最终独立完整审核。MSVC异常注入终止已修复，保留诊断与原始超时；另行补源异常转Result、Watch主体槽回收、管理输入复制前预算以及负枚举反例，具体红绿链见实施者冻结总结。

已启动独立完整核心/规格覆盖复核及根工程35项集成，随后核对五组包装的24条子命令、48份原始流、12份生成源码与4份正控制对象。正式三配置尚未开始，未提交实现为Passed。


根工程开发集成 `evidence/bootstrap/D1.04/integration-e90b09d64c8b` 实际35/35通过，运行前后223项输入无新增/删除/修改；来源摘要 `08ecf883a2a7dd9c14848ad88485917fa7ac23ff405835284fb32cf5eda9c204`。工具独立审核的 `final-tools-integration-audit.json` 核对470项构建ZIP字节、五组24条owned命令/48原始流/12生成cpp/4正控制obj，八项负编译及CoreContracts安装成功、Runtime拒绝均有真实证据。正式采集模式254项匹配且满足全部最低数量。上述为该冻结候选的集成事实，仍不代替完整正式矩阵；完整核心复核正在验证编码返回缓冲的可变元素别名候选，未直接以候选认定缺陷或整包批准。


## 完整复核追加的实际缺口

编码端返回vector的move保留外部元素地址，独立反例 `encoder-alias-3caf9bd610bd` 的合法对照退出0，别名用例退出1：reserve期间旧元素改为0x7e，实际sink收到126而非原81。探针按存活观测避免释放后访问，未将恶意内存写当合同反例。修复在内部Access::transmission以const引用接收并复制到私有帧；复制位于限长之后、锁外和最终提交之前，原返回容器在reserve期间仍存活。`send-tests-35de2c7df405` 五项实际通过，包括响应与Watch两条回归的真实发送字节。公共API和固定35名称保持原样。

独立观察预算反例 `projection-budget-1efdbfdef52a` 进一步确认：基础配置与会话6885字节，新增Entry.operation和独立投影各拥有14字节key文本；仅余14字节时旧实现仍允许get，足额28字节为正控制。这里只计新增独立副本，不给共享不可变source Summary重复收费。该实际少计问题正在修复，尚未关闭。先前Summary共享可变候选经CoreContracts不可复制/赋值和私有构造核实撤回；巨大facts候选也因已有合法Summary的facts上界撤回，不虚构red。

完整规格复核还要求补原计划已有子断言：非Invoke可信required_permissions变更及补齐、分页的委托/owner/phase绑定、合法关闭换源后旧list/Watch跨Store拒绝，以及认证/subscribe异常后小配额无半发行。缺测试不直接记作产品漏洞，补入既有35主体并独立复核后再重新冻结。


## 修复后再次冻结

`implementation-80da80079094` 在预算修复前真实失败，新认证异常与subscribe中途异常小配额重试控制已通过；修后 `implementation-d6b561947b73` 全部35原生主体通过，共38条owned命令均Exited/0、active_after=0。计量现在区分实际独立投影：get/list与新Watch帧计Entry及投影，Watch订阅只计其实际保留Entry，响应排队只追加Entry副本，不重复计共享投影。投影facts仍按实际输出计声明。冻结总结见 `implementation-freeze-9a841021c0ef/review.md`。

原覆盖缺口的新增观察函数已由独立 `observation-coverage-fix-review.md` 限定批准并挂接既有主体。发送别名测试进一步加入真实delete正控制，在缓冲已释放时不解引用旧地址；对修复前独立源码的 `send-alias-red-0085f154b8e1` 实际在原字节断言失败，其他四发送用例通过，新完整35轮验证修后行为。

主任务仅将新观察header的CRLF统一为LF，原/新SHA及仅行尾差异保存 `freeze-after-review/newline-proof.json`。最终三配置224项输入完全一致，摘要 `1a044ad0c5b50f1efa53a82125430a7002a61a1fbbd989f8f1ca2e1e50ac8352`；正在依次运行根工程Debug/Release/ASan各35项开发集成。profile驱动增量经独立 `integration-profile-driver-review.md` 批准，正式251/251/253和九CHECK仍未开始。


最终核心/规格及全部既定覆盖已由 `final-contracts-fixes-review.md` 独立AI Approved；预算同源码独立绿见 `projection-budget-a66771c38322`。局部Debug `integration-debug-75627b76d082` 和Release `integration-release-af5477237b1b` 均35/35通过且来源无变化，归档已分别独立核对。

首轮局部ASan `integration-asan-bc3cce35dc7a` 配置/构建成功，开发驱动额外的直接--list启动失败（Crashed/3221225781），未计通过。构建内discover已带编译器运行库目录成功；同一程序的真实配对控制 `asan-runtime-control-07a51e79705a` 证实未带目录启动失败、带已注册目录则正常枚举35项，进程树均清理。仅修ignored开发驱动，使额外枚举使用该配置已生成CTest材料中的runtime-dir作为子进程PATH；不改全局环境、正式工具或冻结224来源。原/新驱动保存在 `asan-runtime-driver-fix`，完整ASan正在新目录重跑。该失败属于开发验证启动环境，不能写成内核行为失败或篡改历史退出码。


## 提交前局部三配置完成

最终局部ASan `integration-asan-38238230de77` 实际35/35通过，配置、构建、修复后的真实枚举及CTest均正常退出，无残留进程。与前述Debug/Release共105次固定合同执行全部通过；三个来源输入均为224项、摘要 `1a044ad0c5b50f1efa53a82125430a7002a61a1fbbd989f8f1ca2e1e50ac8352`，运行前后集合与字节无变化。ASan首次启动失败及其配对控制继续保留，工具增量批准见 `asan-runtime-driver-fix-review.md`。

实现只涉及内部内存授权Policy及规划要求的验证消费者；内部目标仅依赖CoreContracts闭包，SDK Runtime仍未实现。完整源/测试覆盖独立批准见 `final-contracts-fixes-review.md`；三轮完整归档已由 `final-tools-fixes-review.md` 独立AI Approved，`docs/reviews/D1.04-spec.json` 与 `D1.04-code.json` 已绑定相同224项来源及实际审核SHA，当前准备提交推送。正式完整回归为251/251/253及每配置三CHECK，尚未运行前不将D1.04或G1标Passed。


实现节点 `8978b277850beb98ab9da16838052d6f32a7a4c4` 已提交并推送至任务分支。`evidence/D1.04/source-provenance-8978b27.json` 以git cat-file逐blob核对224项字节与SHA全部一致，来源摘要仍为 `1a044ad0c5b50f1efa53a82125430a7002a61a1fbbd989f8f1ca2e1e50ac8352`。正式Debug已启动，后续Release/ASan串行运行；当前未取得完整矩阵结果。报告的dirty字段保留实际工作区状态，新增证据/进度不改受审输入，不为隐藏证据人为清空该字段。


正式Debug报告 `evidence/8978b277850b-1a044ad0c5b5/win-msvc-debug/D1.04/20260907T194244Z-e3a6ad2e91a8/report.json`（SHA `44e16d1f26dbf890769f4cd83197b5b53a45cc0c0d504a21c4a5493491c50029`）实际251/251、三CHECK全部Passed，automated_status和该run package_status均Passed，errors为空。该配置独立审核正在进行，正式Release已按同8978b27提交及1a044ad0摘要启动；整包仍等待Release与ASan及最终矩阵门禁，不把单配置Passed提升为D1.04最终Passed。


正式Debug独立验收 `evidence/D1.04/debug-acceptance-review.md` 已Approved（SHA `5eca0898618b32b8a53eafd24db42a34a86905efad641ecfdd9e16ca420fc96e`）：完整audit无错误，251项、三CHECK及224个Git blob来源逐字节一致。该结论仅限Debug，不提前替代剩余配置。


正式Release报告 `evidence/8978b277850b-1a044ad0c5b5/win-msvc-release/D1.04/20260907T200010Z-4529dec17f39/report.json`（SHA `8667c58e316f049ff0ec1e260abae1c820fa7dafd32c306dc80ab12e7491c886`）实际251/251、三CHECK全部Passed，automated_status和该run package_status均Passed，errors为空，来源仍为8978b27/1a044ad0。正式ASan已启动；当前合计502次正式CTest、六CHECK通过，但整包仍等待ASan与最终门禁。


正式Release独立验收 `evidence/D1.04/release-acceptance-review.md` 已Approved（SHA `246112ae8ad474459577fbc6a97535b81401239e0574ba3ef65707c9eeaa6d59`）：完整audit无错误，251项、三CHECK及224项来源与Debug一致。整包仍等待ASan与最终门禁。


## 最终正式矩阵与自动放行

| 配置 | 实际CTest | CHECK | 原始报告SHA256 |
| --- | --- | --- | --- |
| Debug | 251/251 | 3/3 | `44e16d1f26dbf890769f4cd83197b5b53a45cc0c0d504a21c4a5493491c50029` |
| Release | 251/251 | 3/3 | `8667c58e316f049ff0ec1e260abae1c820fa7dafd32c306dc80ab12e7491c886` |
| ASan | 253/253 | 3/3 | `23d1a7fd0f5293618f7dd8b533a286f1f931bb7c8ad08582c75a3da31edf9085` |

ASan原始报告为 `evidence/8978b277850b-1a044ad0c5b5/win-msvc-asan/D1.04/20260907T201743Z-19d70286dddb/report.json`；其automated_status与run package_status均Passed，errors为空。三配置共755次单轮CTest及九CHECK；报告全套源码、构建、运行原始流、归档、工具链和AI审核均被正式validator核验。三配置共用实现8978b27及224项受审输入，不混合不同来源。

最终门禁 `evidence/D1.04/current-20260907T203550Z/gate-summary.json` 自动状态和gate_status均Passed，missing/errors为空，SHA `4f1e6b7131e75fbc4a78d7b97a8ff26c23c7e9cc08af7b8bc1bdbe01c0d77e13`。同目录保存真实owned命令、原始流、矩阵、Git来源证明及验收上下文。最终独立AI审核 `evidence/D1.04/final-acceptance-review.md` Approved（SHA `ee9c1ce461ea7ad79e1ac3a27dbdb135dd81c1f7476257226f57720ac0662d9e`），逐报告audit及独立门禁重算同为Passed，未覆盖主门禁或旧失败。

本阶段只完成D1.04内部内存授权与规划验证消费者。SDK Runtime仍不可用，没有生产认证、密码学、网络、执行索引、Invocation/Host或产品模块完成声明。G1仍InProgress；本次验收记录随必要节点提交推送，之后按D1.05前置实际状态继续。
