# D1.05 验证记录

当前包级状态：**Passed**。实现节点 `a8049df35d7428e79d89df9b0f2f223fa7002bda` 的正式三配置共851次CTest、九CHECK全部通过，完整门禁Passed，独立AI最终验收Approved。255项来源与Git逐字节一致，输入摘要 `028ca1c8afab6dfb2b5c3c6758ce4b71f13311957cc78bdc2463e6993a265560`。前置见 [前置记录](../reviews/D1.05-prerequisites.json)，完整结果见末节；下文历史轮次保持原始事实。

范围依据为 v3.3 D1.05、A04/A05/A21。计划见 [D1.05](../plans/D1.05.md)。仅覆盖 Native Read/Compute 及共享治理、受限固定场景分配计数，不扩展 Host、任务、动态入口或产品模块。

## 证据规则

- 候选测试集合与接口通过独立 AI 规格审核后才冻结实施；不从运行发现反填 expected。
- 每次真实运行使用独立目录；启动缺实现、逻辑反例、工具链失败和通过回合分别记录。
- 分配计数注明实际构建/CRT/模块覆盖；首用、预热、稳态、错误与可变结果分列，未覆盖项目不能记零。
- 同一源码提交三配置完整结果、九 CHECK、Git 字节来源及最终独立审核齐全后才记包级 Passed。

## 实施前冻结

[启动记录](../reviews/D1.05-start.json) 绑定 API `31757ff610fb162e639c714f85de9655706b7432632dc5599772cc7d0566d796`、计划 `5437add4bef4cde29ba1d0123e3309361c4260c5125cb025c1a94aeb133ae0ec`、32 主项 expected、三配置及全部前置/审核来源。独立 [API 规格审核](../reviews/D1.05-api.md)、[计划工具审核](../reviews/D1.05-plan.md)、[集合审核](../reviews/D1.05-expected.md) 均为 AI Approved，只批准实施设计。

固定预期 Debug 283、Release 283、ASan 285，合计 851 次 CTest 与九 CHECK；不是运行结果。独立配置核验脚本及摘要保存在 `evidence/bootstrap/D1.05/review-manifests.py`、`review-manifests.json`。原始 D1.04 回归、源输入范围及材料最低项全部保留。

审核明确：Native 成功路径只有无资源声明的 Read/Compute；注册资源 owner 不是调用 lease。非空资源声明在 invoke 明确 ResourceUnavailable，其余不可用 shape 明确 ProviderUnavailable。固定观测环及满载成本在成功调用计数窗口内；首用和不同计数通道的覆盖边界单列，不声称进程全局零分配。

提交前核对发现四份新 run/matrix 与两份审核正文使用 CRLF。已将全部十二份初始批准材料逐字节存入 `evidence/bootstrap/D1.05/pre-lf-freeze/`，只把六份当前文件转为 Git 要求的 LF。[换行增量复核](../reviews/D1.05-newline.md) 独立确认无语义变化，[当前增量启动记录](../reviews/D1.05-start-lf.json) 绑定新 SHA；旧审核中的摘要对应归档原始字节，不改写历史批准内容。

## 实施中回归（未构成包验收）

2026-09-08：Debug 配置已实际编译 Invocation、Registry、Policy、原生消费者和既有合同目标。`T02.native.registered_compute`、`T02.native.registered_read`、`T02.native.invalid_input`、`T06.native.authority_origin` 及 `ock_native_service` 均以退出码 0 完成；该集合只覆盖首批四项，不能替代冻结的 32 个 D1.05 主项。

同日对 D1.04 既有策略回归执行 35 项，JUnit `build/d1.05-dev/d1.05-policy-regression.xml` 记录 `tests=35`、`failures=0`。首次调用在第 8 项后未回传终端输出但测试进程仍继续，后续 JUnit 完整落盘；该编排现象保留为工具记录，不改变通过计数。

本回合将 Native 内联仲裁改为返回服务端收窄后的有效截止时间，并在仲裁成功后才构造 `WorkContext`；固定容量观察环已接入。资源/非 Read shape、完整目标原始身份、全部负例及分配计数仍待实现和验收，因此包状态保持 InProgress。

## 完整实现与复核过程（替代前段当前状态）

内部 Native 已实现真实注册 Read/Compute、candidate_read 的普通 Read 分支、原始身份及每次权限仲裁、目标投影、可信线程准入、有效截止与预算、进入前后失败分界和有界同步观测。每绑定预分配并发槽，调用栈持有治理材料；参数校验重入替换外部绑定不改变当前投影。非 Read shape 明确 ProviderUnavailable，非空资源声明明确 ResourceUnavailable。没有公开 Handler 提取入口，没有 Task/Host 或 Runtime SDK 完成声明。

Policy 的七项独立开发用例通过于 `inline-policy-7bc202b81563`；基础合同/Registry 和 Policy 已独立 AI 复核。`pipeline-82c5130c811c` 保留缺失拒绝观测、绑定配额未回收、业务错误原因错误映射的实际失败；`pipeline-6a2a61e1dc3c` 的对应七项通过。后续 `pipeline-fc96c50794d3` 在旧 Invocation 上实际重现资源上限和总预算溢出反例，两项退出 1；修复后完整主体曾在 `pipeline-749a8016298a`、`pipeline-f2ef27441645` 各通过 32 项。这些是开发主体运行，不能替代八个根工程包装及正式矩阵。

独立审核要求追加真实重叠并发、校验器替换绑定、返回对象最终观测与结果运输控制。旧危险运输尝试 `pipeline-3c335c7a6787` 的 invalid_output 实际 Timeout，owned Job 已清理；同轮另两个旧行为反例实际退出 1。根工程旧轮 `integration-9beaf7aaa98d` 为 31/32，invalid_output 超时 600 秒。两者均不作为异常可恢复或终止原因的证据。新独立依赖 probe `wrapper-transport-ff1c9bc50710` 以 terminate handler 固定标记及退出 86 实证锁定后端边界。

现行运输约束见 `docs/contracts/native-invocation-transport.md`。`pipeline-1e41c14e44e5` 用旧运行时和安全的新 bind 反例实际退出 1；`pipeline-b4ce2aa7a9db` 的 31/32 暴露统一 trait 误拒绝 void。经独立源码复核，void 成功分支没有 R，错误分支只移动 Error，允许该例外并保留所有非 void 限制；新增 void 成功/业务错误/异常、unique_ptr 正例。中间 `pipeline-68a788e232c5` 和 `pipeline-8c074e2c7fea` 分别为测试引用新符号、编辑期间快照不一致的编译失败，不能称逻辑 red。

分配探针分别记录 C++ new 与 Debug CRT，不相加；Release CRT 为 null。增加 usable bytes/块数驻留、窗口峰值、释放守恒、四次有限预热、最后 Bound 持有的内部状态/Catalog/观察存储及 weak 控制块回收。`pipeline-a5d3e6c67ff6`、`pipeline-d45d2ff7b11b` 各四项通过；初轮外壳与内部 EngineState 寿命假设错误保留于 `pipeline-891aa521507c`、`pipeline-2e632b786ff5`。独立规格复核随后要求拆分 calloc/realloc 单入口，以及首用/可变结果精确成功断言；已修改，必须以新三配置实测为准。

当前启动完整 Debug/Release/ASan 集成，每轮独立目录、源码输入清单/归档、32项实际发现、JUnit、八个编译/示例/安装/分配包装及 owned 子进程。完整集成和独立复核完成前不提交为通过；正式 283/283/285 及九 CHECK 仍未运行，D1.05 保持 InProgress。

完整 Debug `integration-debug-61b6f6c6c86d` 与 Release `integration-release-40b5d325d591` 各 32/32；独立复核逐项核对 254 源归档、8 包装/27 子命令和每配置 68 个分配样本。ASan `integration-asan-40c7511005de` 实际 31/32，唯一失败是 allocation_probe 的 `c.crt>0`，owned Job 正常排空。这说明现有 CRT 通道尚不足以证明 ASan 配置的逐入口覆盖；其余稳态用例通过不能补足这个缺口。正在定位入口并审核独立 sanitizer hook 通道，失败原始材料保留，未作豁免。

核心调用管线最终独立规格/代码审核为 AI Approved，见 `native-pipeline-independent-final-20260908.md` 和 `native-pipeline-final-source-binding-20260908.md`，绑定当时 254 输入摘要 `90f1fb73189b4c009ad3cf923f405e52f93f6ad58b6fc569729d009bc92a1291`。该结论范围不包含尚未通过的 ASan 分配覆盖及正式包验收。

ASan 根因由独立同源配对消费者 `asan-crt-diagnosis-581ee85e3e8f` 实证：普通 Debug 的 12 个入口各 CRT=1，ASan 的 12 个入口全部 CRT=0，实际分配均成功；sanitizer 注册返回 1，12 个入口各 sanitizer allocation=1。realloc 释放回调为 2，其余为 1，不假定分配/释放事件一一对应。增量规格 `docs/contracts/native-allocation-coverage.md` 经独立设计复核，要求保留原 CRT、单列 sanitizer 通道及不可见范围、逐入口探针、完整调用窗口和真实注册失败控制；不将新计数混写为 CRT。开发辅助脚本同时补齐空发现拒绝的静态验证缺口，未称为原生逻辑反例。

## 最终实现节点的局部三配置

最终 Debug `integration-debug-474c25032e22`、Release `integration-release-ee8d09f863ec`、ASan `integration-asan-dd8b40540919` 各 32/32，JUnit failures/disabled/skipped 均为 0；各驱动 configure/build/discovery/CTest 均退出 0、owned Job 排空。三份完整 255 项输入与运行前后及当前工作树相同，摘要 `1685e9ccdd20ac4edb07e78249fb119970e7b8a89885490fb565a07ae57ca2df`。主集成者重新读取原始 JUnit、结果清单和当前输入并逐项断言一致。新增 sanitizer 通道在固定全调用窗口仍为零，但仅在已声明参与模块、当前线程、有限预热和小定长无资源场景内成立。

补充独立诊断 `asan-channel-verification-59bf0b8f1efe` 对正式 probe 源码副本和第二个观察 hook 逐入口互校，双初始化不重复计数，真实钩子槽满后正式初始化以 marker 和退出 87 拒绝。未将此前 ASan CRT 盲区或历史 31/32 改写为通过。完整矩阵 283/283/285 和九 CHECK 仍须在已审实现提交上另行运行；以上 96 次仅为本包开发集成，不构成 D1.05 Passed。

## 提交前基础审核闭合

进一步核对发现基础/Registry 原归档明确仅 SPEC，不能按上下文摘要误称 CODE 已归档；已重新要求五个生产文件的独立代码审核。原 SPEC 所列多 Unknown 精确集合反例、helper 等价与非空借用控制也未完整挂接，因此上节 `1685…a2df` 的 96 次通过仅保留为历史来源事实。

已在既定 `outcome_consistency` 中补三条未知事实的精确/置换成功和重复/遗漏/外来/已解析 ID 拒绝，并对空、原始、部分已解析事实集比对新旧 helper；`resource_lifetime` 证明借用不延长租约寿命，过期后不读取或解引用视图。`allocation_other_costs` 单列非空借用上下文构造/有效访问/析构的完整零计数窗口，不将其冒称支持 Native 非空资源调用。

仅上述两测试文件改变，255 输入新摘要 `43ad6c2ecd162908932248ba430ab48c86a94a7d213cfca804cd0e7f972a02ef`。已启动新完整三配置 `integration-debug-eb47edd21d57`、`integration-release-871673f28330`、`integration-asan-f65332a33b8b`；正式矩阵与包验收仍未开始。

上述最终三轮已全部完成，各32/32，零失败、禁用及跳过；主集成再次比较当前255输入和三份源清单，逐项相同。五个基础生产文件独立 CODE Approved 已另行归档 `foundations-independent-code-20260908.md`，并与原 SPEC 的精确 SHA 匹配。各范围最终独立复核及新增子断言的运行核对共同绑定本轮43ad来源，随后登记 `docs/reviews/D1.05-spec.json`、`D1.05-code.json`；这是实现审核，不代替正式包级门禁。

暂存后完整空白检查发现新增运输说明文档末尾多一个 LF，已只删除该空行。最终输入摘要因此为 `028ca1c8afab6dfb2b5c3c6758ce4b71f13311957cc78bdc2463e6993a265560`；与43ad三份实际归档相比，唯一变化是 `docs/contracts/native-invocation-transport.md` 的一个末尾 LF，其余254项包括全部代码/测试逐字节不变。该无语义调整另经独立复核，不把局部运行冒称在028ca输入上执行；正式矩阵将在最终提交与028ca来源上完整运行。

D1.05 首份正式报告已完成：Debug 283/283 CTest、3/3 CHECK，automated_status 与 run package_status 均 Passed，errors 为空。报告 `evidence/a8049df35d74-028ca1c8afab/win-msvc-debug/D1.05/20260907T235925Z-a07bf46d5760/report.json` 绑定实现 a8049df 与028ca输入。Release 已启动，ASan及最终汇总待运行，整体包级仍 InProgress。

D1.05 正式 Release 已完成，283/283 CTest 与3/3 CHECK均通过，报告自动状态及run包级状态Passed、errors为空；来源与Debug同为a8049df/028ca。报告路径 `evidence/a8049df35d74-028ca1c8afab/win-msvc-release/D1.05/20260908T001937Z-799b5f40252d/report.json`。ASan已启动，整体包级仍待最后配置与最终门禁。


## 最终正式矩阵与自动验收

| 配置 | 实际CTest | CHECK | 原始报告SHA-256 |
| --- | ---: | ---: | --- |
| debug | 283/283 | 3/3 | `cd162ff112b27110569381db14bc223748df0ba3b613831c9302afa85cd2acf1` |
| release | 283/283 | 3/3 | `e70a92bc34d039e8e85eb75b3d63680406dc4c2bfead934106c9310a2baa7ab4` |
| asan | 285/285 | 3/3 | `8cc0d7e5ec2f3ebbc4b00bc57df033d475e39e6c780be353a10a61a95c01f9e0` |

三份正式报告位于 `evidence/a8049df35d74-028ca1c8afab/<profile>/D1.05/<run_id>/report.json`，完整路径见 [验收索引](../../evidence/D1.05/current-20260908T010205Z/acceptance-context.json)。三个自动状态及run包级状态均Passed，errors为空，实际JUnit零失败、禁用和跳过。来源均为同一实现提交与028ca摘要；原报告dirty=true保持原样，实际255项Git字节已独立核对，不伪称整工作树干净。

[当前完整门禁](../../evidence/D1.05/current-20260908T010205Z/gate-summary.json) 自动及gate状态均Passed，missing/errors为空，SHA `aa92551ba75d71463ffe1bc7a95b5fb755b3f630298fd44e11eeb7c041613c0f`。[独立最终AI验收](../../evidence/D1.05/final-acceptance-review.md) Approved，SHA `dd5374856dce03ff1981b2d8ed91f0659ba6e938a09a34f5ae351300e471e72e`，独立运行当前gate再次Passed。两类技术审核、原始归档、来源与子控制均核对齐全，没有本包剩余验收条件。

共24个包装、82条包装子命令、207个分配样本。ASan逐入口正控制、无分配负控制、注入分配退出1与真实注册失败退出87均核对通过；ASan原CRT缺失12入口仍保持未覆盖声明，Release不可用通道仍为null。40次完整稳态调用窗口在各配置有效通道中为零；非空借用上下文单独测量，不当作支持Native非空资源调用。首用、错误、可变结果、驻留、释放与峰值分列。

本次只验收D1.05内部Native及规划验证消费者；现行结果运输、资源和Provider边界仍适用，不扩展SDK Runtime、Host、异步或产品模块。历史失败及旧来源原文保留。D1.05 Passed，G1仍InProgress，下一包D1.06须核对D1.05与D0.06全部实际前置。
