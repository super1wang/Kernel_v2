# D1.04 API修订3独立 AI 规格复核

- actor_type：AI；独立复核者：review_contracts。
- 结论：Approved（具体规格准入）；不表示行为实现、测试或包级Passed。
- 已审API：`docs/contracts/policy-api.md`。
- SHA256：`e8a2d6c3f449a88aaef6673609c3b98437e63c35b244c83e4a74a24ee329ecc7`，本轮读取时实际计算一致。
- 对应计划SHA256：`18b29acac29e31c5eb48ffe153c4b273f2c53082f5aaa6d794d93e5a32b35897`。

本轮仅复核源寿命增量，沿用 `D1.04-api-revision2.md` 对其余具体接口的审核；关闭 `../../evidence/bootstrap/D1.04/source-lifetime-review.md` 的追加P2。两份历史报告原文保留。修订3原字节位于 `evidence/bootstrap/D1.04/draft-revision3`。

## 关闭依据

1. API 387明确Store处于Open期间source的host固定、restore恒Absent；同ExecutionRef的owner、精确操作、规范化实际目标及实例不可重绑定。phase/progress等普通观测值仍可演进，不误冻业务状态。
2. API 145/157给出具体 `close_store() -> Result<void>`，以不分配的永久StoreClosed位在消费/发送同一短仲裁中线性化。所有授权入口最终重验；无需逐对象清理完成才让旧材料失效，析构及回收留在锁外，已Consumed/Started事实不回滚。
3. API 387要求合法源切换先完成旧Store关闭，再装配新StoreId的新源。389明确锁外identity读取只是失约检测，不把重复读取当作TOCTOU消除手段。观察到矛盾时关闭，且不引入无界历史表或生产索引。
4. API 461发送最终仲裁核对StoreOpen和Response/Watch绑定源身份；492/494私有状态表保存固定源身份及实际授权事实。故查询后关闭先赢，旧响应/监听帧零字节；发送先赢，真实已开始事实保留。原反例不能在遵守修订3源合同的装配中成立。

没有新增必改项。该结论依赖明确可信source合同，不声称抵抗任意失约/恶意进程内C++适配器。后续实现必须以实际关闭交错、旧对象拒绝、真实字节及观测状态正常演进验证；本轮未运行行为测试。

文档中的PendingReview是送审时冻结状态。最终规格准入可由D1.04-start.json绑定本报告及以上摘要，不需要为改状态而改变受审API字节。
