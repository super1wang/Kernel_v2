# D1.03 API修订2增量 AI 规格复核

- actor_type：AI；review_contracts。
- 结论：Approved（仅本次具体API增量规格，不是代码验收）。
- 已审 `docs/contracts/registry-api.md` SHA256：`ecdd0b6e24c1065fd19e5c27f8f786cc3a096bc1e6c62679314b0eced0893fa5`。
- 修订1的批准报告 `D1.03-api.md` 和原始字节 `evidence/bootstrap/D1.03/registry-api-revision1.md` 保留，不改写历史批准SHA。

本次两项增量符合D1.03既有所有权与声明归属要求：

1. add改为 `const ModuleInput&`，容器复制明确置于入口异常保护内；调用方即使传std::move仍不能把原vector缓冲移入批次。共享owner的控制块允许共享，保存owner的条目必须独立。这样既避免旧元素指针改写候选，也把复制异常纳入已审粘性失败规则。区分入口内异常与入口调用前实参求值异常是准确的，不应让注册器承诺观测尚未调用它的异常。
2. 跨模块重复精确OperationKey声明在钩子前拒绝，每模块完整性按该模块真实成功登记记录核对，不能由其他模块条目代替。本次明确落实了原API全局精确key唯一和模块归属要求，不改变不同精确版本可共存。

实现仍须针对移动输入后原元素改写、提供项owner槽改写、第二模块仅声明重复key但空钩子等反例保存red/green。初读实现的另一个批次文本/声明预算问题记录在 `evidence/bootstrap/D1.03/review-initial-20260907T2330/review.md`，它不是本次两项增量批准所关闭的代码缺陷；后续需明确计费并实现、验证。旧具体API的有限预算规则仍然有效。

本轮未修改产品和测试，没有声明25主体或wrapper已最终Passed。后续代码修复需按新SHA独立复核。
