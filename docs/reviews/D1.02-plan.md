# D1.02 实施准备独立评审

评审者：Codex 独立规格复核代理（actor_type=AI）。时间：2026-09-07T09:07:48.283945+00:00。
审核对象：docs/plans/D1.02.md，SHA-256：`2e45fe3dba30e6604ca8c7d91c1f3b7e298d7ef8a741a99d9a19176d2a19b212`。

结论：修订后的准备稿范围完整，可作为启动门满足后的实施输入；未发现实质规范冲突或偷偷实现后续模块的要求。初审提出的异步深拥有、实际C++类型与可信issuer两个开放点已通过新增小节具体化，足以指导实现。最终API拼写和头文件拆分仍可在内部声明审查时固定，不要求当前已有精确公开ABI。本文不批准越过D1.01及其他前置启动D1.02源码，不声明D1.02已实现或Passed，也不增加人工审批要求。

## 范围与规范一致性

已对照执行卡D1.02、架构A02/A04/A05/A06/A07/A09/A10/A17/A19/A22有关合同，以及D0 Outcome、commit、Intent和Control观察合同。操作精确键、四种shape、受限Context、typed绑定、Caller、九种Outcome、Executor/Completion、Atomic/记录窄端口和类型化观察/枚举均有对应交付与正反例。实际计数固定草案38个名称，无重复；用例尚未实现或运行。

ExecutionRef/PrincipalRef沿共享DTO，Host/registry/epoch与业务ID分离；ASCII名称、精确版本、追加KnownFacts、发布门、Finalizing与可靠完成、观察单调版本及有界枚举参数与既有合同一致。没有以Result替代已发生效果，或以持久提交/自填布尔值冒充已发布。

产品依赖保持CoreContracts→Foundation，生产Registry、认证/许可仲裁、Invocation、Host、调度/线程池、State、SQL/恢复、RPC订阅/cursor均留原后续包。确定性工厂、分页见证和Outcome纯验证是本包端口验证消费者，不构成生产执行表、第二Runtime或Plan解释器。内容较多但均能映射D1.02执行卡；实施时应保持当前限定，不把DTO完整性变为提前生产实现。六个建议头允许内部合并/细化，只需同步最终公开清单与独立包含验证。

## 所有权与可信来源补充审核

异步拥有性现在明确默认Disallowed；可复制/可移动不构成深拥有证明，标准borrowed类型及嵌套借用测试合同不得Submit。只有经可信注册作者审查的Owning carrier或深拷贝适配结果允许进入异步输入；C++20无法反射证明任意struct的全部间接引用，计划如实限定为受审声明与验证消费者。该设计不需要新增生产注册器。

实际CppTypeToken独立于稳定类型名/版本/指纹，由受检定义工厂从Handler真实Args/R推导；两个不同C++类型复用相同稳定描述也须拒绝。token不持久化、不承诺任意DLL ABI。BindingPort及Caller/permit/publication proof的issuer由组合根注入受信实现，普通DTO不能自授有效能力；测试走同一公开协议，无friend/skip后门。D1.03负责真实目录，D1.04及原定提交包负责生产发放与仲裁，职责划分合理。

实施时还应保留两条明确约束：Owning carrier在提交后须保证Args及可达材料只读/冻结或独占移交，不能仅因shared_ptr保活就允许外部可变别名继续改写；工厂生成的CppTypeToken、shape与稳定契约描述应随定义owner冻结，不能由业务调用方回填或修改字段。这是现有不可变输入/可信定义原则的具体化，避免重现D1.01的可变详情别名问题，不增加新模块或要求恶意Native代码沙箱。

## 工作及证据边界

本次仅有界只读审核、38项名称计数及计划摘要绑定，没有运行或实现D1.02测试。D1.01正式采集中的源码、测试、Schema、manifest和历史证据均未修改；仅新增本文，无Git操作。前置包实际Passed、最终配置矩阵、AI规格/代码审核与自动验收仍分别记录，不能用准备评审代替。
