# D1.04 API修订4独立 AI 规格复核

- actor_type：AI；复核者：review_contracts。
- 结论：Approved（仅resolve候选预检增量规格）；不表示行为修复、测试或包级Passed。
- API实际SHA256：`5f6f054582b003f7d2f38a842691545f03d9cadc48f5faf525b47a9bafb79203`。
- 计划实际SHA256：`40527aef2e1082471c6979980c012b7a88b2a72a24cd9db3c0dc67eb85cd4deb`。

本轮读取当前文件并计算摘要，检查第3节新增两段及其与第1节完整tuple规则、A10目标解析/实际权限分层的关系。其余规格沿用D1.04-api-revision3.md及此前独立审核；保留所有历史报告。

新增文字闭合early-core-review/resolve-semantics.md指出的局部歧义：候选必须具有同一已安装use、精确operation、目标及可信完整所需权限。主体ACL、会话当前委托、认证ceiling、精确操作模块、目标规则以及非Invoke用途模块分别由单条规则匹配同一候选，禁止跨来源选不同候选或跨规则拼权限。未知目标与不存在共同候选统一拒绝。

owner/field在目标候选阶段明确不参与匹配，不枚举其他主体或owner，不调用执行元数据源，也不以当前主体充当实际owner。成功只允许返回当前会话的目标身份；prepare、观察和发送仍必须使用真实事实做完整tuple授权。Read操作和非Invoke观察具有正控制，不引入Invoke-only或固定写权限假设。原始发行对象、连接和政策/委托/目标世代重验继续有效。这是独立目标候选可见性合同，不是对执行数据的owner检查豁免。

## 与此前自动拒绝的差异

已读取 `evidence/bootstrap/D1.04/implementation-target-approval-rejection/rejection.txt`。记录中的自动审批拒绝了一个修改TargetPort::resolve并随后运行develop.py的命令：其方案遍历配置中的任意主体作为owner，把allowed的任意匹配作为目标可见性，审核理由是可能扩大未授权主体访问，当前授权没有覆盖其广泛副作用。记录确认该命令未执行，另一个固定当前主体的短暂方案也已撤销。

修订4既不执行也不批准那段被拒绝补丁：它明确禁止枚举owner；目标候选四维检查独立定义，真实owner授权仍在后续完整检查中执行。本次只批准上述文档语义及边界，不把AI规格批准当作自动执行审批的替代，不重跑或修改被拒绝方案。实现必须严格遵守该有限算法并以实际反例验证，若实现重新引入任意owner匹配，不在本批准范围内。

没有新增规格必改项。早期resolve实现缺陷仍待按本合同形成实际red/green并独立代码复核；本报告不提前关闭代码缺陷。
