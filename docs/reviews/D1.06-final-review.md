# D1.06 / B1 集中规格与代码复核

复核者：Codex 主集成 AI，分别履行 SPEC 与 CODE 职责；不是人工批准，也不声称存在未执行的独立代理审核。两项最终 JSON 绑定同一精确来源摘要。包级 Passed 还须真实正式矩阵齐备。

## SPEC 结论依据

- 范围遵循 v3.3-r2：NativeHost 只装配无资源同步 Read/Compute；注册、每次授权、线程/预算/Outcome 治理保留。未装配异步/State/Storage 能力明确不可用，不扩展产品模块。
- Host 停止准入、等待在途、逆序停止模块、关闭日志、最后 owner 释放分别有明确合同。违约同步回调不能抢占；超时/未排空不宣称 Complete。Logging 普通内存易失数据不等同 Audit/恢复事实。
- SDK 使用同一 STATIC Runtime 和安装公开头；模板所需 detail 可以由公开头内部引用，消费者不获得私有 dispatch。原 CoreContracts 阶段、旧暂停指令和过期政策引用已从当前导航纠正。
- 64 个原工作包均具备分级及合并安排；开发批次内小项集中直接测试与 review，正式包仍按 DAG。无关历史模型不进入本次全矩阵。D1.06/G1 使用同三份报告。
- 固定 expected 共 273 个跨配置 ID，适用集合 270/192/193；Debug discovery 已只读核对为 270，无缺项/额外项，未据此生成 expected。正式预算必须在最终新报告前批准，方法身份变动拒绝复用。

## CODE 结论依据

- 检查 `packages/runtime/host/host.cpp`、Host 公共及 detail 表面：准入计数与相位受 mutex 保护；thread-local frame 拒绝自身关闭重入；返回对象生命周期由 admission/owner 覆盖；未排空模块保留依赖，析构 invariant 拒绝带在途资源销毁。
- 检查 `packages/runtime/observability/logging.cpp`：固定记录与有界环、静态字段格式/脱敏、不回绕水位；虚调用外持有 owner，跨方法重入及异常隔离；flush/close 不伪造持久化成功。Host/Logging Debug 32 项直接测试已实际通过。
- 检查 Native SDK 导出与实际消费者：生产翻译单元归属同一 Runtime，公开消费只通过安装树，实际 compile/link trace 验证 CRT/ASan/LTO 和 Runtime/bcrypt 闭包；最终适用矩阵再次直接验证。
- footprint 复用已有唯一进程 owner、协议、采样和分配校验，没有另建进程控制器。新增正式入口核对真实 CL trace、每个 ABBA 对及阶段线程差为零、独立线程入口控制、逐调用分配探针和注入 red、完整原始流/模块身份、阶段窗口、有限预算和不复用 pilot。报告只声称抽样指标，不冒称精确瞬时峰值或物理机器验收。
- 修复校准断言对同一绝对截止两条路径的误判：只接受实际 Timeout 或明确 RunDeadlineReached，并要求本轮 Job 已终止排空；不放宽时限。保留原失败证据。
- 修复正式收集器与 SDK/footprint 输出位置冲突：允许 build、evidence/bootstrap、evidence/G1，仍禁止源码目录、正式报告树、路径逃逸，保留按 manifest、运行前库存差集、逐字节归档与重复/旧材料拒绝。真实正控/篡改/旧材料三项验证通过。
- 本机编译器为已签名的 19.44.35216.0；精确锁、62 项工具字节和宏探针已一致更新，原锁保留。锁反例 15 项通过，Debug 编译及 Release/ASan Runtime 编译通过；新工具链不回写旧包的运行身份。

- ASan 分配 pilot 检出探针 TLS 退出期崩溃；hook 先核对无 TLS 的有效计数线程，再读取局部计数，避免系统线程初始化/退出时访问无效 TLS。修复后实际 injected=1、baseline/native=0 且计数校验通过。未关闭 ASan、未更改生产 Runtime，最终矩阵包含受影响 Native/Host/Logging 计数控制。

## 放行边界

最终 SPEC/CODE 状态与摘要写入各自 JSON；本页记录复核内容，不手写机器测试成功。正式报告、预算及 G1 决策未齐前保持 InProgress。任何新增失败只修复其原因及关联风险；来源变化遵守最终一致性要求，不拼接配置。历史失败、旧工具链和旧暂停记录保持原样。
