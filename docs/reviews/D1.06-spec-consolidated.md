# D1.06 合并 SPEC 复核

- actor_type：**AI**；reviewer：`/root/review_foundations`。
- 日期：2026-09-08；结论：**Approved，限以下精确输入的实现合同与测试职责 SPEC**。
- 复核方式：Host、Logging、SDK 合同及测试职责为独立只读复核；本 reviewer 是 footprint 合同的整理者，该部分明确为**作者自审**，不冒称独立复核。
- 不包含：CODE 批准、正式数值预算批准、实际测试/测量通过、D1.06 包级 Passed 或 G1 放行。本轮没有运行测试、pilot 或完整矩阵。

## 输入绑定

| 文件 | SHA-256 |
|---|---|
| `docs/contracts/native-host-api.md` | `5be3a10c0abaeeaa9242b3c4c90b114522748f8c45c4bbb4a99eba99d666821c` |
| `docs/contracts/logging-api.md` | `007317558b76b499dd6272c95e4637b59eaa443b2b947fa914310a7d7f1651aa` |
| `docs/contracts/native-sdk-surface.md` | `0a71a75f38a0e9512294db8cf3bb0406711115fa5f320efdc87bce61d3b50dda` |
| `docs/contracts/native-footprint-method.md` | `041ecfd7602a194dc4b50f8aa74619485b159dfbcdfed8d6b668beb17fb6e950` |
| `docs/contracts/native-test-scope.md` | `21e7ca1a91e7f29e5b867a4a8eeef59960191ac592d7ae20f165baf86d318918` |

## 一致性结论

**Host 与 A16。** factory/日志/Policy 有状态装配延至 start，返回 backend 先无分配登记，再创建/校验 facade；失败含初始 snapshot/State 分配，资源保留且可显式重试。失败启动沿成功步骤逆序 modules→Policy→logging；正常 shutdown 保持 Finalize 先 close_store 再逆停模块。首次错误、清理错误、截止和 quiescent 分离；未完成依赖保留，PendingCleanup 有实际身份、总数/写入数/截断，不因空输出假报排空。准入与 StopAccepting 同锁，端口/handler/日志锁外；返回构造期间的准入与局部 owner 保活、Session 外壳/显式撤权和禁止 Host 自毁边界一致。

**Logging 与 A15/A22。** 唯一 LogPort/既有 Result，固定格式/默认脱敏、有限容量和满载策略、接受/淘汰计数不混淆。flush 覆盖恒 H，淘汰返回 min(E,H)，幂等不承诺后续淘汰后的字节恒等。close 栅栏、显式重试与 Host 待清理项一致。SafeLogger 局部 State 覆盖异常回程、跨方法/同 backend/16 帧重入及独立 facade 计数；非合格接受后抛不被伪造为已知拒绝。log_error 在 CoreContracts 头内 inline 定义，独立适配器无 Runtime 符号依赖；Host 自身 UnsupportedCapability 用 ock.host，底层读取错误透传 ock.logging。

**SDK 与 A19/D1.06。** dev.2/NativeSubset、真实生产 STATIC Runtime、OCK PUBLIC 闭包仅 CoreContracts/Foundation 与系统 bcrypt LINK_ONLY 如实登记。公开实验性头与安装 detail 明确分类，Registry/Policy/Invocation 保持唯一定义和原真实管线，无新裸 dispatch。BUILD_TESTING=OFF、独立搬迁 C-A、裁剪正负控制与真实链接/运行覆盖安装闭包；未实现组件/异步不冒称可用，dev.1 开发对照不冒称正式旧版或 ABI 兼容。

**footprint 与 A21（作者自审）。** 默认 Host 生命周期日志通过有效 ModuleContext.start 内写/flush及 Host.copy_logs 证明；普通 invoke 不新增普通日志，40 次整窗仍计原治理、事实环和结果析构。准入仅保留到最终返回对象构造，不被计数窗口扩展到调用者析构；结果自身所需 owner 由值合同负责。ABBA、采样、轻量 Release 时延、释放阶段和管理 owner 专项分离；原 owned process 仍为唯一进程 owner，None 分支不顺带改变。0 新线程/0 成功窗分配保持硬约束，未覆盖通道不填零。

## 测试职责可验证性

native-test-scope 将故障窗口与实际可观察结果绑定，包含 facade 创建失败后 backend 清理、模块返回/异常自身 RAII、不同停止路径、pending 截断、真实准入竞态/重入/析构子进程、跨 Host/会话撤权与返回寿命。结果析构的分配窗口与 HostAdmission 生命周期已明确区分，不要求修改 InvokeReply 为持准入容器。

Logging 两个独立合法工厂展开同一五项共同集为十个固定主名 `T22.logging.memory.*` / `T22.logging.test.*`，另列分页和固定写入计数；无 callback 不能跳过寿命共同项。SDK 八项、footprint 方法控制/真实报告分开，模拟护栏不替代实际 OS/子进程控制。Debug 广泛原生影响、Release/ASan 真实声明/寿命/计数专项和配置无关工具项分开；未实施 Host 不由 D1.05 family 自动假覆盖，也不机械重跑所有历史模型矩阵。

以上输入未发现尚未关闭的实质 SPEC 阻断。先前清单中的缺合同、内部装配清理、未排空清单、日志命名/水位/重入、实际测量表面均已在本次输入中收口；本报告不修改旧 Candidate、ChangesRequested 或历史运行记录。

## 尚待实际完成的工作

本批准只允许按这些合同实现及落实固定测试职责。正式 expected/run 的具体 ID、配置、子工件映射仍须根据这些已审职责形成，不能从当轮 discovery 倒推；本报告不声称它们已存在。实现与实际反例/正控、CODE 复核、测量工具/方法配置、pilot、先于最终报告的有限预算决定及预算后新运行仍是后续工作。候选方法中的必填有限期限/采样容量/校准输入必须在运行前具体绑定，不能缺省无限或按结果临时补值。

正式占用 bytes/ms 数值目前未获本报告批准。按原 AI 自动政策在同包内完成，不新增平行审批链。只有实际来源/配置/集合/工件可核验时 D1.06/G1 才可引用同一报告；此前 D1.05 Passed 只证明前置，不替代本次修改后回归。输入内容后续变化需核对对应增量，不能把当前 SHA 的 SPEC 结论当作未来 CODE/运行通过。
