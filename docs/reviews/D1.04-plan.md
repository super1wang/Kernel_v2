# D1.04 计划独立 AI 规格复核

- actor_type：AI；review_contracts。
- 结论：Approved（实施计划）；不是实现或运行Passed。
- 已审计划：`docs/plans/D1.04.md`，SHA256 `ac6893fa304662e74508d5e50296599195bdbb54dd90f85ae5e69f424584f6b6`。
- 对应具体API修订2：SHA256 `4b493389f60812eb5a1b1694aa52ac7e666208aa5204dbfbb7f0a3a67287cc6b`，独立批准见D1.04-api-revision2.md。

计划范围符合唯一执行卡D1.04及A06/A10/A17：真实内存授权、会话委托、目标解析、权限世代、一次许可、观察/分页/发送授权窄接入。明确D1.02/D0.05实际Passed前置和证据绑定，不把其他包的运行结果替代本包前置。OS认证、审批broker、生产索引/协议/后端仍留原后续工作包，SDK Runtime不提前开放。

原五项未定内容已转为具体修订2选择，计划要求先独立审核及固定集合再写行为实现。35项覆盖认证来源、连接隔离、tuple完整匹配、完整组材料、双消费与撤权/取消交错、owner/字段/分页、Watch及独立响应发送起点、Unknown处理、拥有性/预算/异常与内部组件边界。新增子断言归入原主体，不把一次底层编译子过程重复计CTest。

候选算术正确：原216/216/218加35，得到251/251/253，共755次CTest；每配置三CHECK单列。以上是固定集合待运行预期，必须通过真实发现/运行核对，不能从发现反填预期或删除失败项。

反例先行、合法控制、原始red/green分目录、锁定MSVC/owned Job、精确来源及AI规格/代码分开审核、冻结后同提交同摘要矩阵的顺序合理。实际发送验证要求预分配sink字节而非bool，竞态使用受控顺序和实际双线程消费，不依赖sleep猜测。

后续按该范围实现并核验即可，无新增计划必改项。旧初审记录和候选状态保留；此批准不使D1.04自动变为Passed。
