# D1.06 SPEC 适用性增量复核

actor_type: AI  
reviewer: review_foundations（独立复核；未编辑本次合同）  
status: Approved（仅下列适用性澄清）

| 合同 | 原已审 SHA256 | 本次 SHA256 |
|---|---|---|
| docs/contracts/native-host-api.md | 5be3a10c0abaeeaa9242b3c4c90b114522748f8c45c4bbb4a99eba99d666821c | 0220e3c7b75c2d39ce51e8fba5a041478e6d4940734beded399a3b6a46a3ae0f |
| docs/contracts/native-test-scope.md | 21e7ca1a91e7f29e5b867a4a8eeef59960191ac592d7ae20f165baf86d318918 | 11b8b47ec79562331e0a1a2aa95fd57ff0fcce06c20f4b7853dddd6ea53df4a6 |

已独立重算当前两文件及 `evidence/bootstrap/D1.06/spec-applicability-delta/before-*.md` 字节摘要，与同目录 inputs.json 完全相等。before 是经明确逆替换恢复后核对摘要的历史输入副本，不是新发生的旧版运行。逐行差异只有一处 Host 启动描述及两处测试职责描述；原合并 SPEC 报告保持不变。

1. **Native 同步能力澄清成立。** 原公开 API 没有 NativeOnlyExecutor 类型；真实 Registry 拥有外部 ExecutorBinding，Host 只准许 inline_safe、非 async/wait 的 Read/Compute。修订明确 Host 不提供提交入口、不调用 submit、不分配队列或新线程，避免凭未被使用的类宣称能力。测试保留不可用声明启动拒绝并验证实际同步路径 submit 计数为零，意外调用即失败；外部用户端口的独立 submit 行为不冒充 Host 提交能力。符合 v3.3 的 Native 子集与能力如实声明边界，无需引入虚假 Task/Document 或无引用 adapter。
2. **Policy 关闭分支适用性澄清成立。** 已核对当前 PolicyAdministration::close_store 实现：置 closed、锁内 drain、锁外回收，然后返回成功；在 Host 已准入归零且无异步 frame 的上下文，没有受支持的可重复错误返回注入路径。修订保留真实成功、顺序、关闭后拒绝与 owner 寿命测试；错误/异常保留分支仍需源码核对，明确不能报告该分支实际 Passed。没有用不可达 bad_alloc 或测试替身制造虚假关闭故障。

模块与日志的可达关闭错误/异常、Failed 保留与重试、截止、首错、清理容量/截断/饱和、域/code、禁止 what() 泄漏等要求均未删减。64 位边界如何实测不在本次差异中，不将此批准扩大为任何尚未审阅的测试实现或运行结果。

本页是限定两份合同差异的 AI SPEC Approved，不是 CODE、SDK 守卫修复、数值预算、正式运行或 D1.06/G1 Passed。本次未构建或执行测试。
