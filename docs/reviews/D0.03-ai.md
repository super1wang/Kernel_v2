# D0.03 独立 AI 技术复核

2026-09-07，review_d001 对照 A04/A05/A09/A17，只读审查模型、Schema、固定 expected/golden 和原始运行证据，构造内存反例并在修复后重新运行。未修改文件或代替用户签核。

已关闭的必须修复问题：

1. 父 Plan 只检查步骤摘要，可能把失败的必要 child 当作全成功；现检查实际子 Outcome，并禁止父自身 pending 失败升级为 PlanCompleted。
2. 旧 ResolutionRecord 只在出现时核对，允许后追加相反 CommitFact；现对完整事实集合重查历史确定结论。
3. owner 关系可形成环或被字符串哨兵绕过；现拒绝祖先环和多父持有。
4. 父 cancel 未向仍持有的 child 传播；现传播到自有 child，明确转交及迟绑定行为。
5. collect_children 可重写已确定/已记录 Outcome；现只收集真正 pending 的父结果，保留必要记录与结果的一致性。

最终独立复核：43/43 unittest 通过，五项原始反例均拒绝，合法 Plan 路径保持可用；54 golden 分类一致。Executor 拒绝后保留 Accepted 身份并形成 FailedBeforeApply 符合 A09.1，没有将其错误改成删除身份。

review_d002 另审 bootstrap 适配器，发现 unittest 跳过/预期失败会退出 0；修复后真实 runner 的成功、失败、跳过、预期失败、意外成功、缺失用例六项检查全部通过。

无未解决的必须修复项。可延后的防御一致性项：Observer 两种输入入口对异常高版本 Terminal→Running 的拒绝规则可统一；正常模型不会生成这种快照，不阻断本包。正式 Control 观察合同在 D0.04 继续核对。

以上为技术复核，不等于人工批准、真实 Runtime 验证或 G0 Passed。
