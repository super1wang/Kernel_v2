# B2：执行规划补录与 B3 前收口计划

更新：2026-09-09。规范为 v3.3-r2 的 A03/A04/A17/A19/A21、E00/E02 及 D2.01–D2.04。本页于 B2 初次交付后补齐；第一部分是事实补录，不冒充开发前已存在的计划。第二部分在本次修复编码前建立。

## 初次批次规划与交付补录

| 包 | 分级／正式前置 | 生产责任与集中安排 | 完成条件与当前结果 |
|---|---|---|---|
| D2.01 | Standard；D1.01、D0.06 | Payload/View、clone/share/freeze、单 DOM 预算解析、种子同批 | 冻结无可变别名、拒绝重复键/预算超限、ASan 借用逃逸；历史 Passed |
| D2.02 | Standard；D2.01、D1.02、D1.05、D0.04 | FieldSpec/TypeContract、Schema 注册编译、Native/Dynamic 绑定连续开发 | 两入口同判、无网络 ref、只编译一次；历史 Passed |
| D2.03 | Standard；D2.02、D1.03、D1.04 | 目录、命令卡、帮助、分页/指纹集中 | 字段版本同源、权限隐藏、eligible 不替代许可、冷 Docs；历史 Passed |
| D2.04 | Critical；D2.02、D1.04、D0.03、D0.04 | 帧/路由/Outcome、mock 观察、无状态 cursor 集中 | 非法帧拒绝、事实保留、ack-first、cursor/断线边界；历史 Passed，新增收口问题见下 |

原实现 `621d8ea`、交付 `5f8a07c`；正式集合与证据导航见 [B2 交付](../validation/B2-delivery.md)。已通过前置只读取 progress 状态与公开合同，不重读历史审核或重跑矩阵。开发按 Data→Binding→Catalog/Control，批次内接口 Implementation-Ready 即继续，最终 Passed 按上述 DAG。

## 本次 B3 前收口（执行前规划）

输入：用户提供《Kernel_v2_B1-B2_当前状态收口与B3放行文档_2026-09-09.md》。建议经源码核对后采用，不将附件原文视为新的唯一规范。基线 `5f8a07c`，保留历史 Passed；本次为原包的影响修复，不新增第 65 包或 Gate。

| 项 | 决策／生产动作 | 直接验证 |
|---|---|---|
| C-B2-01 | 修复 Subscription 外部 reserve 持锁；短锁预留和复核，可信 start_now 保留短仲裁 | reserve 等待另一线程 close/unsubscribe；队列与租约归零、无越界事件 |
| C-B2-02/03 | 翻页保留首次 issued/expires；认证上下文包含完整连接/委托/权限视图 | 三页 t=110/121、不续命；同主体跨会话、restrict 后在扫描前拒绝 |
| C-B2-04 | ACK/event 的 frame_bytes 都含 12 字节头，ACK 超限完整回滚 | 1024 字节预算＋大合法 filter；watch/lease/queue 清零 |
| C-B2-05 | Runtime/B2Subset 显式生产选择，Runtime 不取得/创建/安装 Data/jsoncons | BUILD_TESTING=OFF 的 configure/build/install/真实 Native consumer；B2 安装仍通过 |
| C-B1-01 | 自定义日志后端必须兑现 Host 请求 limits；沿用失败关闭/重试 | 容量、overflow、level 三种不符及关闭重试 |
| C-B1-02/C-B2-06 | 更新现行合同状态；给 DynamicOnly 执行注册明确后续责任 | 文档/SDK 静态一致性；CLI/MCP 不宣称当前可执行 |
| O-01 | 可随 cursor 修复去除 read 再签名；provider 缓存不作为前置 | 既有 cursor golden/篡改/期限合同 |
| O-02/O-03 | 延后：没有测量支持，不删 HostBound 最终校验、不建立 single-flight 平台 | 后续真实热点触发再规划 |

先补关键反例，再连续修改生产代码。开发只运行 Debug 最小影响集；稳定后针对并发寿命/优化风险集中 Debug、Release、ASan 的 subscription/list/cursor/Host 影响集，构建裁剪和安装专项只按相应 Profile 复用一次。expected 从上表确定，不由发现结果反推；不重跑 D1/G1 或 B2 无关 Data/Schema 全矩阵，不扩建 Evidence 工具。

一次批次收口材料区分 SPEC（约束/范围/方案）和 CODE（实现/反例/结果），引用独立原始运行；只有同一最终来源直接验证与裁剪专项齐备才放行 B3。涉及协议认证输入、生产裁剪与能力责任的决定见 [ADR](../adr/ADR-b2-closure-boundaries.md)。B3 实施规划见 [B3](B3.md)，本轮不实现 Named Pipe/CLI。

集中集合在正式运行前固定：三配置共同执行 `T19.protocol.subscription`、`T20.protocol.list`、`T20.protocol.cursor`、`T03.host.start_failures`、`T22.host.shutdown_errors`、`T19.host.logging_ownership`、`T03.host.native_results` 及 `T24.b2.installed_consumer`；Debug 另执行架构 DAG/负例和 `T24.b2.native_pruning`（含 Runtime-only 真实安装消费者）。合计 Debug 11、Release 8、ASan 8。本次是影响收口，不重新验收历史包；三配置触发是并发/生命周期与公开安装布局，裁剪逻辑专项仅 Debug 一次。
