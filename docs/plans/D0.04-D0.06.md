# D0.04–D0.06 节点实施记录

唯一责任与通过条件仍为执行计划 v3.3 的对应执行卡，本文件只记录落实顺序，不新增包或门禁。2026-09-07 用户回复“继续”，已批准上一轮 D0.02/D0.03 交付；D0.01 亦已批准。

1. D0.04-a：固定 Plan Schema、字段/slot/binding/exports 合同、样例与逐节点正反 golden；D0.04-b：独立 Control 观察 Schema、无状态 MAC cursor 规范与模型，覆盖分页、订阅、授权、gap 与恢复世代反例。两部分保持独立 wire 命名空间。
2. D0.05：实现提交/许可/取消/撤权/publication 及 Intent epoch/claim/GC/恢复小模型，枚举竞争顺序和故障窗口，验证已有 Outcome 的事实口径。不实现真实数据库或 Runtime。
3. D0.06-a：MSVC/C++20 Debug/Release/可支持 ASan presets；按组件取得官方依赖、核对许可证/摘要和实际最小 probe，SQLite 含 WAL-reset 修复；从空 build 复验。
4. D0.06-b：正式证据 runner/validator、Schema、expected/discovered/executed 对照、原始命令/轮次/源码/构建/依赖身份绑定；以失败、空集合、错过滤、旧报告、超时、错二进制、缺轮次等反例自证。历史 bootstrap 保留来源和缺项，只导入可验证事实。
5. D0.06-c：共同合同 harness、mock 合法端口、fault 非法端口与能力适用 manifest；证明必需项不能以可选能力或 NotApplicable 绕过，不声明真实生产后端 Conformance 已完成。
6. 完成包级规格复核与代码质量复核，集成现有 D0.01–D0.03 测试，使用正式采集器生成当前包报告与门禁摘要；提交并推送中文文档和原始证据。只有本批必需材料/测试/人工审查全部具备才可 G0 放行。

独立范围：Plan/Control 文件、commit/dedup 模型文件、依赖 probes/锁文件分别由独立实施者负责；根 CMake、共享测试入口、正式证据工具、Conformance 骨架和总体进度由主集成者负责。冻结后再最终采集；检测到源码变化必须失败重跑，不改历史输出。
