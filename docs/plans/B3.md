# B3：真实 IPC、CLI 与 Shell 执行规划

## G2 后收口执行前补充（2026-09-09）

本节在修改前建立。用户提供的 B1–B3 建议作为待核实输入，不作为新规范。采纳 C1（Watch 终态单调性）与 C2（冻结周期动态材料复用）；暂缓 O1 验证去重、O2/O3 IPC 缓冲优化、Catalog 权限端口拆分与部署 Service SID，避免将正确性修复扩大为验证语义、发送并发或产品部署变更。

实施：Watch 仅从有效完整快照记录终态，拒绝同身份终态回退，旧提示不重开；BoundOperation/InvocationBinding 显式消费预注册材料，服务在 Host 冻结后创建并强持有材料与 Catalog，会话仅绑定自己的 caller/authority/HostBound。Runtime 不增加 Dynamic 依赖，不增加 Registry。此为现有 A03.3/A05.4/A17.5 合同落实，无主架构决策变更。

开发先跑 Debug 直接影响集；最终因公开模板与跨会话共享寿命变化，集中跑 Debug/Release/ASan 的受影响 binding/schema/catalog、watch、CLI/parity/cost 与 SDK 消费集，Debug 增加 Runtime-only 裁剪及依赖负守卫。新增 100 次会话装配复用证明；不运行无修改的 IPC 故障矩阵或 G1/B2 历史累计集。正式 expected 在运行前固定；G1/G2 历史来源不改写，完成后仅新增 post-gate closure 记录。用户授权的 docs/AGENTS.md 删除一并提交。

更新：2026-09-09；执行前规划已建立，B2 前置收口已 Passed，现已开始 B3 生产编码。唯一规范为 v3.3-r2，包卡 D2.05–D2.07 与 E02。B1/B2 历史 Passed 保留。

| 包 | 分级／前置 | 连续开发安排 | 完成验证 |
|---|---|---|---|
| D2.05 | Critical；D2.04、D0.06 | Named Pipe/DACL、OS 对端身份、部分读写/断线、控制优先与慢流 | 实际双进程；身份不能由客户端声明，断线/预算/关闭合同 |
| D2.06 | Standard；D2.03、D2.05、D1.06 | 薄 CLI、UTF-8/参数/错误、意图文件、list/watch 与能力探测 | 真正 CLI→Control→HostBound，不新增业务分派器 |
| D2.07 | Standard；D2.06、D1.05 | 原生/动态双入口与真实 Shell 消费者，统一收口 | 同一业务操作成功/拒绝结果等价；G2 阶段矩阵 |

启动先核对 progress 和当前公开合同；关键身份/并发反例先行。D2.05 接口 Implementation-Ready 后继续 CLI，不逐包制造交付墙。开发默认 Debug + fail-fast S_changed；最终本批同来源/Profile 尽量共用一次物理运行，包责任独立。G2 的 S_gate 由包卡和阶段要求确定，不把 B2 整套历史验证复制成日常前置。

边界：B3 不实现真实 Task/Executor、State/Plan、Durable、GUI 或设备；观察能力如实声明 mock/summary。SharedTypeContract 可执行；任意复杂 DynamicOnly Operation 当前仍拒绝，后续责任见 ADR-b2-closure-boundaries，不能因 CLI 装配变成 eligible。

重大协议、认证、生命周期、组件或责任调整先更新对应 ADR 和规范/本页；简单子项在本页补充步骤即可，不增加逐项审核链。最终结果及放行日期由实际运行后填写，不预写 Passed。

本批执行步骤：先完成 LocalIPC 真实传输直接测试及 OS 身份反例；再以首字节真实完成判据接入观察授权，保留 Unknown 关闭语义，决定见 [B3 传输 ADR](../adr/ADR-b3-pipe-start.md)；随后实现 ControlClient/CLI、真实无状态 Host 消费者和 Shell/双入口验证。生产组件统一由 B3Subset 选择启用，Runtime-only 裁剪保留。

当前三包所需生产接口与直接验证均已达到 Implementation-Ready，尚待正式 Passed。已覆盖实际 SMB 远端正控制/拒绝、OS SID 拒绝、伪角色、首字节撤权顺序、Unknown 隔离、慢流跨连接控制、真实 cmd/Windows PowerShell UTF-8、意图改参、继承忽略位的 Ctrl+C、双入口结果、单次成本和迁移安装链接闭包。此为开发事实，不替代正式矩阵。

正式 expected 在运行前依据上述完成条件冻结于 `tests/manifests/b3.expected.json`：Debug 28 项，Release/ASan 各 26 项，每项一轮。13 项本批 IPC/CLI/双入口测试为共同 S_required；G2 增加绑定、目录、帧、Outcome、观察帧级及当前 Host/SDK 影响验证。Debug 独有架构负守卫与 Runtime-only 真实裁剪；其余配置不重复另造裁剪构建。无旧 G1/B2 累计矩阵。

物理执行使用 D2.07 集成报告承载同来源三配置结果，D2.05、D2.06、D2.07 与 G2 各有自己的矩阵入口，引用同三份原始报告，不改写或复制测试事实。批次 SPEC/CODE 在 B3-review 中分包给独立结论，绑定同一输入摘要；最终逻辑放行依次遵守 D2.05→D2.06→D2.07→G2。E03 负责发现清单对照、执行、构建产物和运行产物归档；自动验收政策不要求人工批准。
