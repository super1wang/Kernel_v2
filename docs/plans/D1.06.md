# D1.06 / B1 当前执行计划

更新：2026-09-08。依据唯一规范[架构 v3.3-r2](../01_Architecture_v3.3.md)与[执行计划 v3.3-r2](../02_Execution_Plan_v3.3.md)。用户本次要求检查环境、纠正文档并继续至 Passed；上次“停止测试、提交后暂停”已结束，原[开发交付记录](../validation/D1.06-development-delivery.md)保留为历史事实。

## 实际起点

- D1.05 已在 `9cd8439` 验收；前置只消费 Passed 状态和公开合同，不重跑其历史矩阵。
- `d8a351e` 已交付 NativeHost、Logging、统一 STATIC Runtime、NativeSubset SDK 与独立安装消费者，不从头实现。
- 本次已完成工具链校准、Debug 编译及 Host/Logging 32 项直接验证，Release/ASan Runtime 编译通过。实际工具版本和环境见[工具链说明](../toolchain.md)。
- 本轮起点为 D1.06/G1 `InProgress`（最新状态见 progress）：只补 footprint 正式测量、有限预算、最终来源三配置和集中 SPEC/CODE。

## 本批次剩余工作

| 工作 | 收口条件 |
|---|---|
| 测量正式接线 | 既定 11 个逻辑入口，复用唯一进程 owner、计数与校准；Debug 方法控制只执行一次，由同轮各职责核验 |
| pilot 与预算 | 每适用模式 3 个 ABBA 块，独立 AI 根据阶段需求和真实 pilot 决定有限预算；不自动乘系数抬线 |
| 正式矩阵 | 同一最终来源，固定 S_required：Debug 270、Release 192、ASan 193 项和各 3 个 CHECK；预算后每模式 6 块新进程，不复用 pilot |
| 集中收口 | 真实 SPEC/CODE 分别记录，同摘要；D1.06 与 G1 引用同三份报告，全部条件满足才 Passed；提交并推送当前分支 |

## 执行方式

规范清楚就继续实现和调试。相关小项组成一个开发批次，先补直接反例，开发默认 Debug + fail-fast 的 S_changed；按寿命、分配、优化及编译传播风险补 ASan/Release。稳定后直接进行适用正式矩阵，不再先复制一遍全三配置“预验收”。

工具链/公共编译选项、公开合同、测试注册/夹具/证据规则变化需要对应影响回归；只说明具体触发与集合，不升级为整套历史重验。此次 SDK/测量目录与收集器冲突、ASan 探针 TLS 崩溃属于真实阻断修复；其他 Fixture/Evidence/Install Consumer 性能优化不再作为前置。

expected 来自规范与完成条件，不从 discovered 生成或按失败删项。所有失败追加保存；正式来源变化按影响及最终一致性规则处理，不能拼接不同版本成功配置。报告仅证明 NativeSubset，不能代替完整 Embedded 或机器验收。

恢复只读 progress 顶部、本页、当前相关合同/diff及[集中审核](../reviews/D1.06-final-review.md)；按异常再展开原始日志。64 个包的分级、跨包合并与 DAG 规则只维护在执行计划 E00/E02/E04，不在本页复制。预算、SPEC/CODE、自动结果和包级状态分开记录，不新增人工审批节点或政策文档。

历史工具优化结果保留于[审核索引](../reviews/D1.06-index.md)，不重新执行其工作清单。
