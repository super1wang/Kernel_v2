# D1.06 开发批次与测试审查策略

更新：2026-09-08。用户要求按新版提速方案修正后续策略，本文件的流程规则立即生效；行为API、正式expected及工具实现尚未冻结/完成，不声明D1.06 Passed。

依据：[提速方案 v2](../Kernel_v2_开发提速与Token精简决策方案_v2_2026-09-08.md)、唯一[架构 v3.3](../01_Architecture_v3.3.md#a16)和[执行计划 v3.3](../02_Execution_Plan_v3.3.md#d106)。自动验收继续采用既有AI政策，不新增工作包编号或人工节点。

## 当前事实

D1.05在 `9cd8439a1fd9d95bb4062e04f3f6ab7bb4d781b9` 完成验收并推送；283/283/285正式CTest和九CHECK已通过，不重开、不重跑。D1.06已完成前置核对并存在Host/Logging/footprint候选草稿，尚无行为实现或正式验收；因此采用progress中的InProgress（设计阶段），不沿用新版报告中较早的NotStarted描述。G1保持InProgress。

## 先完成有限工具优化

归属既有D0.06测试设施职责，服务当前D1.06；工具优化单独形成可审查提交，不混入Host生产行为。

| 顺序 | 修改范围 | 最低验证和结束条件 |
|---|---|---|
| 1 | `tests/compile/contracts/verify_children.py`及其CMake接入：同run/Profile共享配置，优先CTest Fixture | 身份错配、准备失败残留、工具链失效、两个反例分别build/诊断、真实正控制、少跑拒绝；记录一次可比范围的前后命令次数和耗时 |
| 2 | `tools/dev/verify.py`薄入口及显式映射 | Debug默认、fail-fast、直接及必要传递影响；未知路径扩大当前包；full必须有触发原因；不输出包级Passed |
| 3 | `docs/ai-current.json`非规范导航 | 真实HEAD、规范SHA、当前包与progress核对；不一致即stale并回退完整读取，不用静态stale=false冒充校验 |
| 4 | 正式run的`summary.json` schema/generator | 从实际报告生成，完整证据保留；摘要篡改、缺项及digest不符不能使失败变Passed；不可用字段写明未知而非填零 |

夹具只复用准备，不复用成功结论或跨run二进制。身份包括源、编译器/toolset/SDK、架构、配置、CRT/ASan、公共选项、依赖锁和测试支持输入。同构建树初版确定性串行；各反例仍独立命令、退出码、诊断与日志。

`tests/install_consumer/verify_contracts.py`深度复用仅在编译夹具已取得收益、安装消费者仍是主要热点时实施，否则登记后续项；正常安装根只读，裁剪/缺头/搬迁各用隔离副本，不改为源码include。

只补直接相关工具反例和代表性真实运行；工具变化属于扩大验证触发，但先确定受影响集合，不因此无条件重跑D1.05已验收矩阵。下一轮本来必需的正式验证补充整体耗时，不能为证明提速额外要求多轮全矩阵。不同机器/来源/集合只作分项比较，不宣称严格加速比例。不得编造Token节省百分比。

上述有限项完成并取得可核验收益后停止基建优化，回到内核；30分钟是目标而非通过条件。P1/P2、跨run缓存、通用调度、自动相关性推理、归档迁移不成为G1新前置。

## D1.06作为一个内部开发批次

Host最小生命周期、Logging共同合同、NativeSubset测量和独立安装消费者按依赖顺序持续实现。每个逻辑项保留测试先行的反例/正控制，运行增量影响集后继续下一项；不逐项建立完整设计—三配置—审查—验收循环。公共合同、SDK/CMake、寿命等风险边界做独立专项验证，但不另立包级Passed。

| 层级 | 测试选择 | 可声明结果 |
|---|---|---|
| L0开发快跑 | S_changed：Debug新增/直接/必要传递影响集，风险专项；失败立即返回修复 | 仅局部事实 |
| L1包开发稳定检查 | 当前包Debug全影响集，公开头消费者；按风险补ASan/Release，构建变更补干净构建/安装 | 仅开发事实，不是正式矩阵前置复制 |
| L2包级正式验收 | 稳定最终来源、预先审核的S_required和适用Profile | SPEC/CODE及实际gate齐全后才可包Passed |
| L3阶段门禁 | S_gate：G1真实消费者、安装、关键跨包回归和有限占用预算 | 只放行实际NativeSubset，不冒称完整Embedded |

影响映射使用实际仓库路径，不能照搬方案里的示意目录：`packages/runtime/host/**`关联Host/Native消费者；`packages/runtime/observability/**`关联Logging/Host；`packages/contracts/**`和`packages/foundation/**`关联相应公共合同及消费者；`cmake/**`、`sdk/**`关联构建/编译边界/安装；`tools/evidence/**`关联证据反例与真实报告核验。正式映射必须包含测试文件变更及传递影响；未知退回当前包全量，未覆盖的广泛风险扩大阶段回归并记录原因。

开发full升级仅限新版第4.1节条件；记录`改动→风险→扩大集合`即可，不另建批准材料。内存安全变化优先对应ASan专项，不自动等同所有历史测试全跑。

D1.06新增正式集合不能直接复制D1.05全部历史项，也不能从发现列表反推。根据D1.06要求、公共表面变化及消费者影响集中审核S_required，同时明确S_gate。当D1.06与G1需要的测试重合时，在现有工具支持且来源/配置/集合对应可核对的条件下引用同一真实报告；不能假定未实现的跨包分区工具已存在。未有受审等价规则前保持原最终输入绑定。

## 审核与上下文

每包一个主索引指向最终SPEC、CODE、expected、实际run摘要、gate及必要历史amendment。计划/API/expected作为一次SPEC输入；实现按CODE职责检查。可共用规范区段、diff、构建身份和证据摘要上下文，但分别给出真实结论，不能复制自检假装两类审核。修复后检查变更及关联风险，未变材料用摘要引用，历史记录不改写。

现有候选草稿仅供合并收口，不重新生成多套平行设计。当前默认阅读顺序是progress最新段、可验证导航、相关规范章节、当前合同、diff和证据摘要；导航/摘要尚未实现时直接读取必要来源，不能假装命令已可运行。失败、缺项、异常或审核疑点再展开原始日志；完整机器audit不省略。

## 当前落实情况

- [x] 微批次、最小影响集、四层验证和集中审查规则写入当前计划与AGENTS。
- [ ] Compile Contracts共享夹具及最小反例、前后实测。
- [ ] dev-fast显式映射与实际验证。
- [ ] ai-current导航及stale校验。
- [ ] Evidence摘要生成、校验及反例。
- [ ] D1.06行为API与适用expected集中冻结、实现及正式验收。

本次只是策略修订，不将待实施工具列为已交付，不改变D1.05、D1.06或G1验收结论。用户后续继续实施时按上述顺序执行。
