cmake_minimum_required(VERSION 3.25)
project(OCK VERSION 0.1.0 LANGUAGES CXX)
set(OCK_SDK_VERSION "0.1.0-dev.3")
set(CMAKE_CXX_EXTENSIONS OFF)
set(CMAKE_MSVC_RUNTIME_LIBRARY "MultiThreaded$<$<CONFIG:Debug>:Debug>DLL")
set(OCK_DEPENDENCY_COMPONENTS "Data" CACHE STRING "Explicit dependency component selection")
include(cmake/Dependencies.cmake)
if(NOT TARGET OCKThirdParty::expected)
  message(FATAL_ERROR "Foundation requires the locked expected dependency component")
endif()
include(cmake/TargetDependencies.cmake)
ock_write_target_graph("${CMAKE_BINARY_DIR}/ock-target-graph.json")
include(CMakePackageConfigHelpers)
configure_package_config_file(cmake/OCKConfig.cmake.in "${CMAKE_BINARY_DIR}/OCKConfig.cmake" INSTALL_DESTINATION lib/cmake/OCK)
write_basic_package_version_file("${CMAKE_BINARY_DIR}/OCKConfigVersion.cmake" VERSION ${PROJECT_VERSION} COMPATIBILITY ExactVersion)
install(EXPORT OCKTargets NAMESPACE OCK:: DESTINATION lib/cmake/OCK)
install(FILES "${CMAKE_BINARY_DIR}/OCKConfig.cmake" "${CMAKE_BINARY_DIR}/OCKConfigVersion.cmake" DESTINATION lib/cmake/OCK)
install(DIRECTORY packages/foundation/include/ DESTINATION include)
install(DIRECTORY packages/contracts/include/ DESTINATION include)
install(DIRECTORY packages/runtime/include/ DESTINATION include)
install(DIRECTORY schemas/catalog/ DESTINATION share/ock/schemas/catalog)
install(FILES sdk/sdk_api_manifest.json DESTINATION share/ock)
include(CTest)
if(BUILD_TESTING)
  find_package(Python3 3.11 REQUIRED COMPONENTS Interpreter)
  option(OCK_BUILD_B2_TESTS "Enable B2 production contract tests" ON)
  if(OCK_BUILD_B2_TESTS)
    add_subdirectory(tests/unit/data)
    add_test(NAME T24.b2.installed_consumer COMMAND "${Python3_EXECUTABLE}" -X utf8 "${PROJECT_SOURCE_DIR}/tests/install_consumer/verify_b2.py" --build "${CMAKE_BINARY_DIR}" --config "$<CONFIG>")
    add_test(NAME T24.b2.native_closure COMMAND "${Python3_EXECUTABLE}" -X utf8 "${PROJECT_SOURCE_DIR}/tests/install_consumer/verify_native.py" --build "${CMAKE_BINARY_DIR}" --config "$<CONFIG>" --case installed_host --evidence-task B2)
    set_tests_properties(T24.b2.native_closure PROPERTIES TIMEOUT 600 LABELS "B2" WORKING_DIRECTORY "${PROJECT_SOURCE_DIR}")
    set_tests_properties(T24.b2.installed_consumer PROPERTIES TIMEOUT 600 LABELS "B2" WORKING_DIRECTORY "${PROJECT_SOURCE_DIR}")
  endif()
  option(OCK_BUILD_FOOTPRINT_TESTS "Enable formal NativeSubset footprint tests" OFF)
  if(OCK_BUILD_FOOTPRINT_TESTS)
    configure_file(tests/tools/footprint/formal-tests.cmake.in "${CMAKE_BINARY_DIR}/footprint-tests.cmake" @ONLY)
    set_property(DIRECTORY APPEND PROPERTY TEST_INCLUDE_FILES "${CMAKE_BINARY_DIR}/footprint-tests.cmake")
  endif()
  add_executable(ock_sdk_metadata_probe tests/compile/sdk_metadata.cpp)
  target_link_libraries(ock_sdk_metadata_probe PRIVATE OCK::Foundation)
  add_test(NAME T24.sdk.version_header COMMAND ock_sdk_metadata_probe)
  get_filename_component(_ock_compiler_dir "${CMAKE_CXX_COMPILER}" DIRECTORY)
  set_tests_properties(T24.sdk.version_header PROPERTIES ENVIRONMENT_MODIFICATION "PATH=path_list_prepend:${_ock_compiler_dir}")
  add_test(NAME T01.architecture.dependency_dag COMMAND "${Python3_EXECUTABLE}" -X utf8 "${PROJECT_SOURCE_DIR}/tools/architecture/check.py" --graph "${CMAKE_BINARY_DIR}/ock-target-graph.json")
  add_test(NAME T01.architecture.negative_guards COMMAND "${Python3_EXECUTABLE}" -X utf8 -m unittest discover -s "${PROJECT_SOURCE_DIR}/tests/architecture" -v)
  set_tests_properties(T01.architecture.negative_guards PROPERTIES WORKING_DIRECTORY "${PROJECT_SOURCE_DIR}")
  add_test(NAME T24.sdk.install_and_missing_components COMMAND "${Python3_EXECUTABLE}" -X utf8 "${PROJECT_SOURCE_DIR}/tests/install_consumer/verify_baseline.py" --build "${CMAKE_BINARY_DIR}" --config "$<CONFIG>")
  set_tests_properties(T24.sdk.install_and_missing_components PROPERTIES TIMEOUT 180)
  set_tests_properties(T24.sdk.version_header T01.architecture.dependency_dag T01.architecture.negative_guards T24.sdk.install_and_missing_components PROPERTIES LABELS "D0.02")
  option(OCK_BUILD_EXECUTION_MODEL_TESTS "Enable D0.03 reference-model tests" OFF)
  option(OCK_BUILD_G0_TESTS "Enable all implemented G0 contract and evidence tests" OFF)
  if(OCK_BUILD_EXECUTION_MODEL_TESTS OR OCK_BUILD_G0_TESTS)
    execute_process(COMMAND "${Python3_EXECUTABLE}" -X utf8 "${PROJECT_SOURCE_DIR}/tools/testing/model_cases.py" --emit-cmake "${CMAKE_BINARY_DIR}/D0.03Tests.cmake" RESULT_VARIABLE model_discovery_result)
    if(NOT model_discovery_result EQUAL 0)
      message(FATAL_ERROR "D0.03 model discovery failed")
    endif()
    include("${CMAKE_BINARY_DIR}/D0.03Tests.cmake")
  endif()
  option(OCK_BUILD_DEPENDENCY_PROBES "Enable locked development-only dependency probes" OFF)
  if(OCK_BUILD_DEPENDENCY_PROBES)
    include(cmake/Dependencies.cmake)
    include(cmake/DependencyProbes.cmake)
    ock_add_dependency_probes()
  endif()
  if(OCK_BUILD_G0_TESTS)
    add_test(NAME T01.requirements.traceability COMMAND "${Python3_EXECUTABLE}" -X utf8 "${PROJECT_SOURCE_DIR}/tools/requirements/check.py")
    add_test(NAME T01.requirements.negative_guards COMMAND "${Python3_EXECUTABLE}" -X utf8 -m unittest discover -s "${PROJECT_SOURCE_DIR}/tests/requirements" -v)
    set_tests_properties(T01.requirements.traceability T01.requirements.negative_guards PROPERTIES LABELS "D0.01" WORKING_DIRECTORY "${PROJECT_SOURCE_DIR}" TIMEOUT 60)
    add_test(NAME T23.adapter.contract_guards COMMAND "${Python3_EXECUTABLE}" -X utf8 -m unittest discover -s "${PROJECT_SOURCE_DIR}/tests/tools/bootstrap" -v)
    set_tests_properties(T23.adapter.contract_guards PROPERTIES LABELS "D0.06" WORKING_DIRECTORY "${PROJECT_SOURCE_DIR}" TIMEOUT 90)
    execute_process(COMMAND "${Python3_EXECUTABLE}" -X utf8 "${PROJECT_SOURCE_DIR}/tests/plan/run_cases.py" --emit-cmake "${CMAKE_BINARY_DIR}/D0.04Tests.cmake" RESULT_VARIABLE plan_discovery_result)
    execute_process(COMMAND "${Python3_EXECUTABLE}" -X utf8 "${PROJECT_SOURCE_DIR}/tools/testing/g0_cases.py" --emit-cmake "${CMAKE_BINARY_DIR}/G0Tests.cmake" RESULT_VARIABLE g0_discovery_result)
    if(NOT plan_discovery_result EQUAL 0 OR NOT g0_discovery_result EQUAL 0)
      message(FATAL_ERROR "G0 actual test discovery failed")
    endif()
    include("${CMAKE_BINARY_DIR}/D0.04Tests.cmake")
    include("${CMAKE_BINARY_DIR}/G0Tests.cmake")
  endif()
  option(OCK_BUILD_FOUNDATION_TESTS "Enable D1.01 Foundation contract tests" ON)
  if(OCK_BUILD_FOUNDATION_TESTS)
    add_subdirectory(tests/unit/foundation)
    foreach(_case public_headers installed_consumer missing_dependency_rejected backend_mode_rejected)
      add_test(NAME T24.foundation.${_case} COMMAND "${Python3_EXECUTABLE}" -X utf8 "${PROJECT_SOURCE_DIR}/tests/install_consumer/verify_foundation.py" --build "${CMAKE_BINARY_DIR}" --config "$<CONFIG>" --case "${_case}")
      set_tests_properties(T24.foundation.${_case} PROPERTIES TIMEOUT 600 LABELS "D1.01" WORKING_DIRECTORY "${PROJECT_SOURCE_DIR}")
    endforeach()
  endif()
  option(OCK_BUILD_CORE_CONTRACTS_TESTS "Enable D1.02 CoreContracts tests" ON)
  if(OCK_BUILD_CORE_CONTRACTS_TESTS)
    add_subdirectory(tests/compile/contracts)
    add_test(NAME T01.contracts.component_closure COMMAND "${Python3_EXECUTABLE}" -X utf8 -m unittest tests.architecture.test_contracts_stage.ComponentClosureTests -v)
    add_test(NAME T01.contracts.public_include_boundary COMMAND "${Python3_EXECUTABLE}" -X utf8 -m unittest tests.architecture.test_contracts_stage.PublicBoundaryTests -v)
    set_tests_properties(T01.contracts.component_closure T01.contracts.public_include_boundary PROPERTIES LABELS "D1.02" WORKING_DIRECTORY "${PROJECT_SOURCE_DIR}" TIMEOUT 90)
    foreach(_case public_headers installed_component_consumer install_pruning_rejected)
      add_test(NAME T24.contracts.${_case} COMMAND "${Python3_EXECUTABLE}" -X utf8 "${PROJECT_SOURCE_DIR}/tests/install_consumer/verify_contracts.py" --build "${CMAKE_BINARY_DIR}" --config "$<CONFIG>" --case "${_case}")
      set_tests_properties(T24.contracts.${_case} PROPERTIES TIMEOUT 600 LABELS "D1.02" WORKING_DIRECTORY "${PROJECT_SOURCE_DIR}")
    endforeach()
  endif()
  option(OCK_BUILD_REGISTRATION_TESTS "Enable D1.03 internal registration contract tests" ON)
  if(OCK_BUILD_REGISTRATION_TESTS)
    add_subdirectory(tests/contract/registration)
  endif()
  option(OCK_BUILD_AUTHORIZATION_TESTS "Enable D1.04 internal authorization contract tests" ON)
  if(OCK_BUILD_AUTHORIZATION_TESTS)
    add_subdirectory(tests/contract/authorization)
  endif()
  option(OCK_BUILD_NATIVE_TESTS "Enable D1.05 internal native invocation contract tests" OFF)
  if(OCK_BUILD_NATIVE_TESTS)
    add_subdirectory(tests/contract/native)
  endif()
  option(OCK_BUILD_HOST_TESTS "Enable D1.06 Host contracts" OFF)
  if(OCK_BUILD_HOST_TESTS)
    add_subdirectory(tests/contract/host)
  endif()
  option(OCK_BUILD_LOGGING_TESTS "Enable D1.06 Logging conformance" ON)
  if(OCK_BUILD_LOGGING_TESTS)
    add_subdirectory(tests/conformance/logging)
  endif()
  option(OCK_BUILD_NATIVE_SDK_TESTS "Enable D1.06 installed NativeSubset consumers" ON)
  if(OCK_BUILD_NATIVE_SDK_TESTS)
    foreach(_case metadata public_headers installed_host no_tests_producer link_closure install_pruning_rejected private_dispatch)
      set(_prefix T24)
      if(_case STREQUAL "private_dispatch")
        set(_prefix T02)
      endif()
      add_test(NAME ${_prefix}.native_sdk.${_case} COMMAND "${Python3_EXECUTABLE}" -X utf8
        "${PROJECT_SOURCE_DIR}/tests/install_consumer/verify_native.py" --build "${CMAKE_BINARY_DIR}" --config "$<CONFIG>" --case "${_case}")
      set_tests_properties(${_prefix}.native_sdk.${_case} PROPERTIES LABELS "D1.06" TIMEOUT 1200 WORKING_DIRECTORY "${PROJECT_SOURCE_DIR}")
    endforeach()
    add_test(NAME T01.native_sdk.surface_guard COMMAND "${Python3_EXECUTABLE}" -X utf8 -m unittest tests.architecture.test_native_sdk tests.install_consumer.test_native_wrappers -v)
    set_tests_properties(T01.native_sdk.surface_guard PROPERTIES LABELS "D1.06" TIMEOUT 90 WORKING_DIRECTORY "${PROJECT_SOURCE_DIR}")
  endif()
  get_property(_ock_tests DIRECTORY PROPERTY TESTS)
  set_property(TEST ${_ock_tests} APPEND PROPERTY ENVIRONMENT "MSBUILDDISABLENODEREUSE=1")
endif()
