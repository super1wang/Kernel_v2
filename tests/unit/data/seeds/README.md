# Payload parser 种子

`accepted/` 是可接受的严格 JSON；`rejected/` 覆盖解码后重复键、数值溢出、UTF-8/代理项、截断、尾随内容和深度预算。

`T04.data.parser_seeds` 消费这些原始字节，并对每个字节做一次有界高位翻转，验证接受结果可重新编码/解析。该测试是确定性种子回归，不声称已执行长时间覆盖率引导 fuzz。后续 fuzz 引擎可直接复用这些文件。
