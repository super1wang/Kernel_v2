add_executable(ock_ipc_peer peer.cpp)
target_link_libraries(ock_ipc_peer PRIVATE OCK::Adapter::LocalIPC)
add_test(NAME T20.ipc.two_process COMMAND "${Python3_EXECUTABLE}" -X utf8 "${CMAKE_CURRENT_SOURCE_DIR}/two_process.py" "$<TARGET_FILE:ock_ipc_peer>")
set_tests_properties(T20.ipc.two_process PROPERTIES TIMEOUT 45 LABELS "B3;D2.05")
add_executable(ock_ipc_observation_peer observation_peer.cpp)
target_include_directories(ock_ipc_observation_peer PRIVATE "${PROJECT_SOURCE_DIR}")
target_link_libraries(ock_ipc_observation_peer PRIVATE OCK::Control OCK::Adapter::LocalIPC)
add_test(NAME T19.ipc.authorization_start COMMAND "${Python3_EXECUTABLE}" -X utf8 "${CMAKE_CURRENT_SOURCE_DIR}/observation.py" "$<TARGET_FILE:ock_ipc_observation_peer>")
set_tests_properties(T19.ipc.authorization_start PROPERTIES TIMEOUT 45 LABELS "B3;D2.05")
add_executable(ock_ipc_pressure_peer pressure_peer.cpp)
target_link_libraries(ock_ipc_pressure_peer PRIVATE OCK::Adapter::LocalIPC)
add_test(NAME T22.ipc.backpressure COMMAND "${Python3_EXECUTABLE}" -X utf8 "${CMAKE_CURRENT_SOURCE_DIR}/pressure.py" "$<TARGET_FILE:ock_ipc_pressure_peer>")
set_tests_properties(T22.ipc.backpressure PROPERTIES TIMEOUT 45 LABELS "B3;D2.05")
add_test(NAME T20.ipc.remote_rejection COMMAND "${Python3_EXECUTABLE}" -X utf8 "${CMAKE_CURRENT_SOURCE_DIR}/security.py" "$<TARGET_FILE:ock_ipc_peer>")
set_tests_properties(T20.ipc.remote_rejection PROPERTIES TIMEOUT 45 LABELS "B3;D2.05")
get_filename_component(_b3_runtime_dir "${CMAKE_CXX_COMPILER}" DIRECTORY)
get_property(_b3_ipc_tests DIRECTORY PROPERTY TESTS)
set_tests_properties(${_b3_ipc_tests} PROPERTIES ENVIRONMENT_MODIFICATION "PATH=path_list_prepend:${_b3_runtime_dir}")
