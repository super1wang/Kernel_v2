set_property(DIRECTORY APPEND PROPERTY CMAKE_CONFIGURE_DEPENDS "${CMAKE_CURRENT_SOURCE_DIR}/conformance_manifest.json")
file(SHA256 "${CMAKE_CURRENT_SOURCE_DIR}/conformance_manifest.json" _b4_conformance_sha)
add_executable(ock_executor_contract contract.cpp)
target_compile_definitions(ock_executor_contract PRIVATE OCK_EXECUTOR_MANIFEST_SHA="${_b4_conformance_sha}")
target_link_libraries(ock_executor_contract PRIVATE OCK::Adapter::CpuPool)
target_include_directories(ock_executor_contract PRIVATE "${PROJECT_SOURCE_DIR}")
add_test(NAME T12.executor.conformance_manifest COMMAND "${Python3_EXECUTABLE}" -X utf8 "${CMAKE_CURRENT_SOURCE_DIR}/verify_manifest.py" "$<TARGET_FILE:ock_executor_contract>")
set_tests_properties(T12.executor.conformance_manifest PROPERTIES TIMEOUT 30 LABELS "B4;D3.01")
foreach(backend cpu_pool controlled inline)
  foreach(case ownership_transfer exactly_once rejection_cleanup exception_boundary drain_shutdown worker_rejection queue_full)
    add_test(NAME T12.executor.${backend}.${case} COMMAND ock_executor_contract ${backend} ${case})
    set_tests_properties(T12.executor.${backend}.${case} PROPERTIES TIMEOUT 20 LABELS "B4;D3.01;ExecutorConformance")
  endforeach()
endforeach()
foreach(case real_parallel stop_timeout inline_reentrancy controlled_order)
  add_test(NAME T22.executor.${case} COMMAND ock_executor_contract supplemental ${case})
  set_tests_properties(T22.executor.${case} PROPERTIES TIMEOUT 20 LABELS "B4;D3.01")
endforeach()

get_filename_component(_b4_runtime_dir "${CMAKE_CXX_COMPILER}" DIRECTORY)
get_property(_b4_local_tests DIRECTORY PROPERTY TESTS)
set_tests_properties(${_b4_local_tests} PROPERTIES ENVIRONMENT_MODIFICATION "PATH=path_list_prepend:${_b4_runtime_dir}")
