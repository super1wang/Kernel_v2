add_executable(ock_executor_lifecycle lifecycle.cpp)
target_link_libraries(ock_executor_lifecycle PRIVATE OCK::Adapter::CpuPool)
target_include_directories(ock_executor_lifecycle PRIVATE "${PROJECT_SOURCE_DIR}")
foreach(backend cpu_pool controlled inline)
  add_test(NAME T22.executor.${backend}.worker_destruction COMMAND "${Python3_EXECUTABLE}" -X utf8 "${CMAKE_CURRENT_SOURCE_DIR}/verify_fatal.py" "$<TARGET_FILE:ock_executor_lifecycle>" ${backend})
  set_tests_properties(T22.executor.${backend}.worker_destruction PROPERTIES TIMEOUT 25 LABELS "B4;D3.01")
endforeach()
add_executable(ock_executor_consumer consumer.cpp)
target_link_libraries(ock_executor_consumer PRIVATE OCK::CoreContracts)
add_test(NAME T12.executor.concurrent_duplicate COMMAND ock_executor_consumer)
set_tests_properties(T12.executor.concurrent_duplicate PROPERTIES TIMEOUT 20 LABELS "B4;D3.01")

get_filename_component(_b4_runtime_dir "${CMAKE_CXX_COMPILER}" DIRECTORY)
get_property(_b4_local_tests DIRECTORY PROPERTY TESTS)
set_tests_properties(${_b4_local_tests} PROPERTIES ENVIRONMENT_MODIFICATION "PATH=path_list_prepend:${_b4_runtime_dir}")
