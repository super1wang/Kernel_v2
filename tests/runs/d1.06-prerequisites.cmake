# 仅供候选正式run与L0 full使用；不是生产配置，也不是空测试。
# footprint11真实入口、完整采样与预算前后证据尚未冻结。
# 完成后须在同一次最终SPEC/CODE增量中修订本阻断，不能以缓存或开关绕过。
message(FATAL_ERROR "D1.06 formal candidate blocked: footprint formal entry points and independently approved finite budgets are not frozen; no formal matrix may run yet")
