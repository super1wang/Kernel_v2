# 汇总校验记录

2026-09-14，10 份原方法正式报告均为 Passed，无正式采样失败或重跑。

第一次新增汇总校验退出 1：`verify.py:35 AssertionError: injected`。该汇总程序错误地要求所有命令退出 0；原方法 `tools/footprint/formal.py` 明确规定 allocation 的独立注入反例退出 1，且必须完成受控进程与观测协议。此处是归档校验逻辑错误，不是产品、预算或正式采样失败。初次错误由执行工具返回；本段为事后记录，不冒充原始日志。

汇总校验已按原方法恢复严格规则：仅该命名注入命令允许退出 1，同时检查进程树正常回收、完整阶段、采样与分配反例记录；其余命令必须成功。第二次校验退出 0，原始输出见 `verification-02.log`。原始测量与预算均未更改。

原始报告中的绝对路径指向测量时的本机环境，安装产物、消费者及系统 DLL 的哈希清单按原方法保留；消费者和系统环境依赖的本机路径不能视为跨机器可执行路径。原始日志、命令、样本、方法输入、预算绑定与报告均入库。
