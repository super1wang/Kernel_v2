# v2 开发诊断

F1/F2/F3/F4 旧行为反例均失败；F5 的普通 expiry 被误分为取消也由 corrected-red 复现。最终新反例、Host 资源等待取消与 30 项 Debug 直接回归通过，正式 31 项另外包含 State 安装消费者。

`v2-direct-f4-green` 与首次 `v2-direct-f5-red` 先遇到新增调用导致旧测试计数基线偏移；纠正测试计数后才形成 F5 的真实 red。另一次直接集子进程误用默认 CMake 3.29，保留原始失败，锁定 PATH 后 30/30。所有这些是开发诊断，不拼入最终正式验收。

此目录脚本是当时位于 build 下使用的脚本存档，路径假设保持原样，不作为独立可重放安装包。
