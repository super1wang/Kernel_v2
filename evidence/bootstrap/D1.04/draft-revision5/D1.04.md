# D1.04 授权主体、目标解析与许可实施计划草案

状态：**PendingReview**。仅保存待独立AI规格审核的设计与候选测试集合；没有授权据此写行为实现，没有D1.04实际Passed。具体接口见[policy-api](../contracts/policy-api.md)。本计划只细化唯一v3.3执行卡D1.04，不新增或重新编号工作包。

修订2：按初次AI规格审核五组ChangesRequested补齐API及本表子断言；初版保留，等待独立增量审核。本次没有新增测试主项。

修订3：仅补固定source寿命和close_store仲裁。旧Store Open期间host/Absent和同ExecutionRef的授权事实不可重绑定；合法切换必须先关闭旧Store，再创建新Store。仍PendingReview，保留历史审核，不增加35个测试主项或生产索引。

## 前置与范围

规划前置为D1.02和D0.05，主任务已逐原始报告SHA核对实际Passed，最终启动记录须绑定这些来源，不能用本段替代证据。D1.03现有验收节点271e926及完整216/216/218回归作为当前基线，不把它新增为D1.04规划依赖。

交付`packages/runtime/policy/`、`tests/contract/authorization/`及真实许可交错测试。内部库`ock_policy_internal`仅依赖`OCK::CoreContracts`闭包；使用可信安装的精确操作政策目录，不链接Registry。SDK Runtime保持不可用，不安装内部头或库，不实现Invocation/Host、设备、GUI或D2/D3生产模块。

最小能力是内存四方授权、每连接固定认证authority、显式会话委托、真实目标发行与重验、完整组材料、一动作一许可的短仲裁、取消/撤权先后，以及查询/分页/观察队列到真实发送起点的窄授权接入。观察元数据来自组合根固定的有界源端口和确定性消费者，不建设生产执行索引/RPC。真实无状态cursor/HMAC、完整notification协议、OS管道、批准broker及递归委托留后续既定包。

## 待审文件与行为边界

- `policy.hpp/.cpp`：私有发行记录、完整拥有型政策/委托/组快照及预算；每连接Caller/Targetauthority；每准备动作独立PermitAuthority；固定可信元数据源与发送起点适配。
- 原生`ock_policy_tests`：固定名称`--list`逐行发现，单名称真实执行；异常CHECK不受Release NDEBUG关闭。
- `tests/contract/authorization/CMakeLists.txt`、`discover.py`及编译正反控制：每配置注册；实际caller/permit/target或内部记录提取编译拒绝先有同工具链正控制，不增加隐藏CTest计数。
- 根CMake只在测试开启时接内部组件；真实安装正控制仍能消费CoreContracts，Runtime明确未实现。
- `tests/manifests/d1.04.expected.json`、三份正式run manifest及matrix，在API/名称独立审核后、行为实现前冻结；禁止从发现或结果反填expected。

认证只信任PolicyStore创建时固定的认证端口，外部principal/role/trusted/approved和自制authority不能注入。每连接独立，ServicePrincipal不偷借用户会话。有效权限逐操作/permission/真实目标求主体ACL、会话委托、安装模块政策、目标规则交集。

PermitBinding不变；其单target只投影组anchor，内部保存全部成员、顺序、精确版本/contract、全部目标/实例/生命周期。取消、消费、撤权、会话失效和授权世代更新在同一短仲裁内决定先后，不把ACL、生命周期与资源锁混成一种保护。消费成功仅表示获准尝试。

发送端必须预持有/预留缓冲容量，仲裁内同步非阻塞开始传输，区分NotStarted/Started/Unknown；不能先check再任意异步send。确定性接收sink实际写入字节证明Started起点，禁止只改bool自证。仲裁内无分配、任意析构、业务回调或阻塞I/O；后端不满足即不能装配。

## 建议固定新增集合（尚待审核冻结）

以下35项是首轮前建议集合，不是已运行名称或通过记录。审查发现缺项时在首次行为运行前显式调整并重新审核。

| 名称 | 必须实际验证的反例与正控制 |
|---|---|
| T07.policy.authentication_source | 自报身份/标签/假authority或grant拒绝，配置凭据验证成功 |
| T07.policy.session_isolation | 同Principal不同连接的view/target/action互换失败，各自合法控制成功 |
| T07.policy.service_principal | 服务独立身份和期限成功，断线用户会话不可转服务 |
| T07.policy.scope_four_way | 四个来源任一不足即拒绝，完整tuple交集成功；同源(A,read)/(B,write)不可跨规则拼成(A,write)，owner/field同样不能拼接 |
| T07.policy.delegation_shrink | 收缩生效、旧授权不复活，扩张或延长期限拒绝 |
| T07.policy.recursive_delegation_denied | 请求再委托明确拒绝，首层受限委托成功 |
| T07.policy.owner_scope | owner只是过滤，self/显式授权代表owner对照，无管理旁路 |
| T07.policy.target_issued_identity | 自制/跨authority/跨连接目标拒绝，真实发行目标成功；无权B与不存在C均TargetUnavailable，合法A成功；仅Read操作或仅GetSummary/ReadResult完整候选可解析，跨来源/跨规则候选不拼接；解析成功不代替完整操作/owner/field授权 |
| T07.policy.target_lifecycle | 删除重建及目标世代变化拒绝旧视图和旧permit |
| T07.policy.target_frozen_set | 输入/选择后改不漂移实际目标，多目标缺一拒绝 |
| T07.policy.operation_exact_contract | 未安装/错版本/错contract拒绝，不相信请求所需权限；同Store既有Key改Digest以ContractMismatch原子拒绝且原permit/response仍有效，相同Digest的ACL更新与新版本Key安装为正控制，源未知Key拒绝 |
| T07.policy.group_members_complete | 有batch权限缺一成员仍拒绝，完整成员全部实际检查 |
| T07.policy.group_substitution | 换成员/顺序/目标/摘要碰撞不能借旧准备材料扩大范围 |
| T07.policy.permit_origin_binding | 同字段伪许可/另一动作真许可/替换context原permit拒绝 |
| T07.policy.permit_once | 一动作一个发行owner，消费后重复消费/再发行拒绝 |
| T07.policy.permit_concurrent | 实际双线程同permit仅一次消费成功，不依赖sleep |
| T07.policy.revoke_before_consume | 撤权先赢，实际消费与效果接收计数为0 |
| T07.policy.consume_before_revoke | 消费先赢保留获准尝试，不虚称已应用或回滚 |
| T07.policy.cancel_arbitration | 取消与消费两顺序真实仲裁，外部尚未处理信号不自称胜出 |
| T07.policy.expiry_boundary | 服务器期限取最小，等于截止即失效，无溢出延寿 |
| T07.policy.generation_exhaustion | 降低可信装配identity_limit/generation_limit真实触达生产同一饱和分支；close_store后新Store身份不复用且不认旧发行对象，撤销重授不复活旧材料 |
| T07.policy.owned_inputs_budget | move元素/共享可变输入、空控制块owner及全部嵌套预算正反例 |
| T07.policy.exception_atomicity | 分配/适配异常无半发行、半消费、半监听与泄漏 |
| T19.policy.query_field_projection | 用途独立且从UsePolicyInput取所需权限；完整tuple投影，get后撤权或close_store/source合法替换，旧response实际0字节；普通progress变化仍允许 |
| T19.policy.page_owner_authorization | 非法owner在扫描前拒绝，隐藏项/总数/错误不泄露 |
| T19.policy.page_binding_isolation | 跨连接/委托/过滤/host/期限绑定失败；source.restore非Absent拒绝；未先关闭却自发切换的失约源被检测、StoreClosed且旧响应0字节，检测不冒充TOCTOU保护 |
| T19.policy.page_revoke_live_keyset | 页间撤权逐项复验，有限扫描空页前进，实时非一致语义 |
| T19.policy.subscription_scope_atomic | 全filter逐项授权，失败无部分Watch，无长期缓存allow |
| T19.policy.subscription_connection_cleanup | A失效只清A观察资源，不取消B连接或业务执行 |
| T20.policy.queued_revoke_drop | 初检允许后撤权、合并旧字节、出队前撤权或close_store均0新发字节；get/list真实ResponseAuthorization及Watch分别覆盖源关闭/替换，不假造Watch |
| T20.policy.transmission_start_arbitration | 只读projection/bytes及reserve材料正编译控制；两种确定顺序以预分配sink真实字节证明起点，预留不等于已发送 |
| T20.policy.failed_start_not_started | 容量不足/明确零字节失败可拒绝，重试必须重新授权 |
| T20.policy.unknown_start_no_retry | 不能证明是否已发送则隔离/关闭，无自动重复帧 |
| T20.policy.unsubscribe_inflight | 同连接退订先封新排入；跨连接/旧世代false；已开始字节不宣称撤回 |
| T20.policy.internal_component_boundary | 实际目标依赖闭包、CoreContracts安装正控制及Runtime未实现拒绝 |

若35项审核通过，完整期望为Debug251、Release251、ASan253，共755次CTest，每配置三CHECK。该算术是候选验收预期，不代表发现或实际通过数。

## 实施顺序（审核后才执行）

1. 绑定前置原始报告/门禁SHA，独立AI审核本计划、policy-api及固定集合；审核下节修订内容并冻结全部C++字段/签名与三配置matrix。
2. 先写真正合同反例和正控制，保存无实现bootstrap red；认证伪造、四方交集、组多目标、消费/撤权、队列撤权先写可观察计数与字节断言。既有实现缺陷必须有实际red→green，不把工具链失败当行为red。
3. 实现拥有型政策预算、可信认证、每连接Caller/Target发行和委托收缩；先验证输入元素别名、错issuer/会话、目标重建及异常清理。
4. 实现完整组准备与单动作PermitAuthority；消费/取消/撤权短仲裁；实际并发双消费及受控顺序验证。独立AI复核不得用字段比较替代发行身份。
5. 实现窄观察授权与拥有型队列接入；真实预分配sink证明发送起点及撤权/退订顺序。保持完整cursor/notification与生产索引范围外。
6. 接入原生每配置发现与编译负例，真实安装边界验证；Debug/Release/ASan局部验证及独立AI规格/代码审核。源码冻结后更新中文进度/范围/证据，再按授权本任务分支英文工作包提交推送。
7. 同提交/同源码摘要跑固定完整矩阵、九CHECK、源码Git字节核对和当前门禁；任何失败追加保留、修复后重新冻结，不删预期。全部实际Passed后才更新D1.04包级状态并核对D1.05前置。

配置、构建、编译正反例和进程交错均用既有owned WindowsJob证据工具，原始stdout/stderr、命令、源码、配置/产物留独立回合。锁定MSVC的隔离副本/路径已核验规则保持；不改全局环境、不杀其他进程、不覆盖旧报告。审核actor明确AI，不写human Approved；无需逐节点人工确认。

## 修订2待增量审核的冻结内容

1. UsePolicyInput、逐use维度矩阵和完整tuple匹配已写入API；scan前owner检查明确只是有限候选，不能代替每项四方授权。
2. GroupSnapshot已公开只读envelope/anchor/有序members；固定确定性digest消费者不声明生产密码学，强制碰撞仍不得绕过完整发行材料。
3. PageBindingData字段完整；D1.04只接受source声明无restore的内存模式，Present明确拒绝，未来持久source须独立补充真实恢复身份。
4. PreparedTransmission已给出const bytes/binding/projection读取、私有工厂；固定编码端及start端reserve RAII owner在锁外准备，真实start_now保持短仲裁。get/list通过独立ResponseAuthorization和enqueue_response进入同一发送起点。
5. Administration全部写签名、Session两个单继承端口、observations、Watch/Response工厂及不可复制规则、私有State必需字段和无循环owner关系已补齐。诊断、订阅和终结记录的预算/回收口径明确。

本节记录已作出的具体设计选择，不授予实现者变更它们的自由。独立增量审核通过、全部签名及固定集合冻结后才进入行为源码；发现新缺口则先修正文档，不能用测试夹具降低合同。

修订3补充：`PolicyAdministration::close_store() -> Result<void>`与consume/start同一短仲裁永久设置StoreClosed；全部授权入口最终重验，未消费/未开始材料失效，已Consumed/Started事实不回滚，owner在锁外清理。create保存固定source host/Absent，Response/Watch保留其绑定；锁外identity读取只检测失约，真实源必须遵守“先关闭后切换”，不得靠重复锁外读取关闭变更窗口。无需增加Host、执行索引或新协议。

修订4仅澄清无操作参数的resolve候选预检：同一已安装use/精确operation/目标/可信所需权限必须被各来源的一条完整规则共同覆盖，不枚举其他owner，不读取执行元数据；未知owner/field留待后续实际完整tuple授权。此处不扩大操作或数据权限，不增加签名或35个主项。局部歧义与实际无权目标反例已保存，增量AI审核通过前暂停该处行为修复，其余冻结范围继续执行。

修订5限定既有OperationKey→ContractDigest寿命稳定，防止只有Key的执行摘要在新查询时被当前目录重新解释。可信初始source/目录必须对应执行真实契约；已有Key改Digest失败不消耗generation，相同Digest政策变更允许，新契约用新版本Key。仍不改变任何签名或35主项；仅该新增约束待增量AI审核后实现，历史批准保持原字节。
