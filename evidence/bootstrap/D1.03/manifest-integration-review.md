# D1.03 注册wrapper与归档集成中期复核

actor_type=AI，review_tools。结论：当前实际Debug注册产物布局和三配置归档映射可接受；源码仍在修订，最终代码/规格/清单联合批准待源冻结及补充正控制实际运行，不将本意见作为无条件最终Approved。

已独立读取integration-ece6db1b65的registration-artifacts.json/zip，208项集合、每项size及SHA均一致。5个wrapper共23个实际子命令，46个stdout/stderr引用均存在于归档且SHA一致；各命令active_after=0。分布为cold_docs_separation 1、manifest_owned_budget 4、shape_compile_contract 7、internal_component_boundary 4、handler_not_exposed 7。

208项中6项属于build_outputs（注册测试exe、内部lib、CTestTestfile、发现总入口、配置发现文件、registry-target配置JSON），其余202项为child runtime材料。独立将Debug integration根路径映射到三份正式路径，并对Release替换实际配置目录及配置后缀，每一项均被build_outputs或validator相同Path.match接受，0未覆盖。Release/ASan这里只核对路径模式，没有声称它们运行过本批注册测试。

三清单minimum与这轮实际材料一一核对：5份commands、5份structure、23份stdout和23份stderr、12份cpp、3份positive.obj、1份实际安装OCKConfig、CoreContracts/Runtime各1份CMakeLists及1份target-metadata均满足。最低计数对应固定5个wrapper及9个编译反例，未把旧批产物算成本轮新增；正式collector仍须使用自己的本轮新增路径集合。

wrapper控制逻辑使用既有WindowsJobObject。编译负例前先同包含集正控制成功；各负例检查实际MSVC错误类别及源文件名。内部组件边界先运行native，再从实际生成target metadata核对CoreContracts/Foundation闭包，执行真实cmake install，真实CoreContracts find成功后验证Runtime find以具体诊断拒绝；安装配置和两消费工程源码已收集。cold结构提取与native冷值控制共同覆盖当前结构，不把字符串检查单独作为完整行为证明。

已知待修：manifest_owned_budget中的missing_freeze反例当前共享的positive仅编译Registrar四shape，没有对ConfigurationBinding::make提供相同语义的合法冻结器正控制。主任务已明确实现者正修此点；应保留当前成功轮，另以修订后合法冻结器控制及缺冻结器拒绝实际运行闭合。修后若增加cpp/命令/obj数量，归档模式应继续覆盖全部新增文件，最低计数按最终固定驱动核对；不得覆盖本轮旧证据。

机器可读清单SHA及208项映射结果见manifest-integration-verification.json。本次未发现其他必须新增的工具/归档修改；类型身份预算修复属于并行代码审核范围，本意见不越权批准该行为。
