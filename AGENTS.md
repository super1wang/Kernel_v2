# 内核开发约束

- 与用户沟通默认称呼“老大”，项目文档使用中文。
- 唯一规范为 `docs/01_Architecture_v3.3.md` 与 `docs/02_Execution_Plan_v3.3.md`，先读 `docs/progress.md`。
- 仅推进内核及规划要求的验证消费者；不扩展 GUI、CAD/CAM、设备等产品模块，不迁移旧代码或数据。
- 每次核对包的全部前置实际 Passed；自动结果、包级状态和复核来源分别记录，禁止虚构 Passed。
- 新增行为先增加对应反例/合同测试；实际运行记录到独立证据目录，失败重跑不覆盖。
- Skills 按需使用，不强制每次调用流程技能；明确简单操作直接执行。
- 用户已授权按必要节点本地提交及推送至 https://github.com/super1wang/Kernel_v2；仅推送本任务分支，不强推，不覆盖其他历史。
- 提交前同步中文进度、范围和证据；提交说明使用带工作包编号的英文。
- 若使用 SVN，提交说明必须为纯英文。

- 用户已明确要求“自我复核和自动验收，无需人工”：后续使用AI规格复核、AI代码复核及自动验收，不逐节点请求人工批准。政策见 `docs/reviews/automatic-acceptance-policy.json`；AI审核不得写成human Approved，历史报告不改写。

## 开发提速与 Token 策略（2026-09-08 起）

- 执行 `docs/Kernel_v2_开发提速与Token精简决策方案_v2_2026-09-08.md`；它调整开发流程，不替代两份 v3.3 规范。当前执行细化见 `docs/plans/D1.06.md`。
- 同一工作包相关小项默认组成一个开发批次；批次内先补对应反例，默认 Debug 最小影响集、fail-fast，按寿命/分配/模板/优化等风险补 ASan 或 Release。不为每个小项重复全量测试、审核和提交。
- 开发期不得仅以“更保险”为由运行无关 full CTest 或完整三配置。扩大范围须记录具体触发：构建/工具链/SDK/依赖/公共编译选项、无法窄覆盖的公共合同、测试注册/夹具/发现/expected/证据工具变化、映射未知或漏测、审核发现跨包风险、跨配置问题，或当前确为正式验收/阶段门禁。
- 开发执行 S_changed；包级正式执行受审 S_required；阶段门禁执行 S_gate。不得机械累计全部历史用例，也不得临时删改已冻结集合；新集合须先审核且不能从当轮发现反推。工具未支持的跨包分区或跨 Profile 公共运行不得手工拼接成 Passed。
- 严格 Passed 前置不得批次化越过。跨包批次须满足新版方案第5.3节，保持各包 expected、审核和状态独立；共享运行不等于共享 Passed。
- 稳定后集中执行适用正式矩阵，不要求预先完成同样的局部全三配置。最终来源必须一致；保留失败和原始证据，不拼接不同版本的成功配置。
- SPEC/CODE 保留职责不同的两类真实结论、同一精确来源；共享一次必要上下文准备，计划/API/expected集中作为SPEC输入。变更只做有依据的增量复核，不为每个子步骤新增审批链。
- 恢复任务先读 progress 顶部、当前包索引（可用时）、相关规范章节/合同和 diff；默认不重复读取全套规范与历史证据。索引缺失、HEAD/规范摘要/包状态不一致时回退完整规范和最新进度，刷新后再依赖导航。
- Evidence摘要工具就绪前不得假定已有 summary；就绪后摘要仅作导航，失败/缺项/摘要不符/异常/疑点再展开原始材料。机器完整门禁继续执行，不以摘要替代证据审核。
- 简单操作直接处理，不为省Token建设通用平台或默认多代理。复杂独立任务才按需委派，交接使用相关文件、合同与验收要求，避免重复搬运全部历史。
